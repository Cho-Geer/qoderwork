# Fix Report (M3) — cross-platform-universality-m1

**Date**: 2026-08-03
**Implementer**: M3 (general-purpose)
**Status**: 待主会话复核 (Self-Check Gate only; no Final Gate / Accept signed)

## Executive Summary

Applied EXACTLY the 6 frozen BLOCKING fixes from `2026-08-03-review-m3.md` and `2026-08-03-review-glm52.md`, plus the two minimal follow-ups the progression validator itself required to reach exit 0 on the startable phases (see Fix-6 sub-note). Files touched: `00-plan-index.md`, `01-phase-runtime-import-fix.md`, `02-phase-skill-universalization.md`, `03-phase-entrypoint-optional.md`, `04-phase-ci-and-docs.md`. No NON-BLOCKING / DEBT / OUT_OF_SCOPE fix was applied. `canonical-requirements-contract.yaml` and `approval-decision.json` were NOT modified — approval binding is intact (SHA-256s verified unchanged, no re-approval trigger).

## Fix-1: BLOCKING-001 (Phase 1 §3 regex)
- File: `plans/cross-platform-universality-m1/01-phase-runtime-import-fix.md:50`
- Before: `p=re.compile(r'''from '/home/zhaoge\|require\('/home/zhaoge\|import\('/home/zhaoge''')` — raw triple-quote makes `\|` a LITERAL pipe in regex (no alternation), and only matches single quotes → returns `0 []`.
- After: `p=re.compile(r'from ["\047]/home/zhaoge|require\(["\047]/home/zhaoge|import\(["\047]/home/zhaoge')` — real alternation plus both quote styles (`\047` = `'` in `re`); Result cell annotated "6 files (regex matches both `'` and `"` quote styles; `\047` = `'` in `re`)". Shell-escape level is `\"` inside the double-quoted `python -c`, which bash unwraps to `"` inside the Python single-quoted raw string.
- git diff: `-| 6 unique files contain static \`/home/zhaoge\` import | \`python -c "...p=re.compile(r'''from '/home/zhaoge\|require\('/home/zhaoge\|import\('/home/zhaoge''')..."\` | 6 files |` → `+| 6 unique files contain static \`/home/zhaoge\` import | \`python -c "...p=re.compile(r'from ["\047]/home/zhaoge|require\(["\047]/home/zhaoge|import\(["\047]/home/zhaoge')..."\` | 6 files (regex matches both \`'\` and \`"\` quote styles; \`\047\` = \`'\` in \`re\`) |`
- Verified-by:
  - Exact command extracted from the markdown cell, run via bash: `python -c "import os,re; d=r'C:\Users\USER\ZCodeProject\qoderwork\.worktrees\check-plan\scripts'; p=re.compile(r'from [\"\047]/home/zhaoge|require\([\"\047]/home/zhaoge|import\([\"\047]/home/zhaoge'); fs={f for r,_,fs in os.walk(d) for fn in fs if fn.endswith('.ts') and (f:=os.path.join(r,fn)) and p.search(open(f,'rb').read().decode('utf-8','ignore'))}; print(len(fs), sorted(fs))"` → `6 ['..._d3_live.ts', '...cleanup-regress.ts', '...diag-handover-path.ts', '...diag-schema.ts', '...regress-parent-child.ts', '...test-hybrid-enforcement.ts']`, `rc=0` — exactly the 6 in-scope Phase-1 files.

## Fix-2: BLOCKING-002 (Phase 1 §7/§10 gate)
- File: `plans/cross-platform-universality-m1/01-phase-runtime-import-fix.md:151-168` (§7 step 2), `:214` (§10 gate)
- Choice: **Option A** — the gate is re-expressed as "0 import hits in the 6 named files" with an explicit 7-item residual allowlist. Option A is the cleaner choice vs Option B because Phase 1's file inventory (§5) is contractually exactly 6 files; pulling `diag-handover-path.ts:7` / `regress-parent-child.ts:15` into Phase 1 would cross into P1-DEC-003 env-fallback territory and widen the file inventory.
- Before: step 2 walked ALL `scripts/*.ts` with `p = re.compile(r'/home/zhaoge')` and gated `c == 0` — unsatisfiable (51 hits in 33 files; 34 hits outside Phase-1 scope). §10 gate claimed "Python scan of `scripts/.ts` returns 0 hits".
- After: step 2 iterates the 6-file inventory with the import-scoped regex `from|require\(|import\( ["\047]/home/zhaoge`, counts `import_hits` vs `residual_hits`, prints `RESIDUAL_ALLOWLIST`, exits `0 if import_hits == 0 else 1`. §10 gate now reads "Python scan of the 6 in-scope files returns 0 import hits" with the allowlist spelled out: `diag-handover-path.ts:7`, `regress-parent-child.ts:15`, `_d3_live.ts:23` = `OPENCODE_ROOT` env fallbacks (P1-DEC-003 pattern); `_d3_live.ts:35,36` = `git -C /home/zhaoge/...` fixtures (P1-DEC-002); `_d3_live.ts:9,10` = comments; other scripts/.ts files are explicitly NOT scanned (out of Phase-1 scope, covered by the Phase-4 combined scan).
- git diff: see the `01-phase-runtime-import-fix.md` hunk replacing `-p = re.compile(r'/home/zhaoge')` + whole-directory walk with the 6-file import-scoped scan, and the §10 gate line replaced from `- [ ] Python scan of \`scripts/.ts\` returns 0 hits ...` to the allowlist form.
- Verified-by:
  - Pre-implementation run of the exact new step-2 command: `import_hits=10 residual_hits=7`, `RESIDUAL_ALLOWLIST=['_d3_live.ts:10', '_d3_live.ts:23', '_d3_live.ts:35', '_d3_live.ts:36', '_d3_live.ts:9', 'diag-handover-path.ts:7', 'regress-parent-child.ts:15']`, `rc=1` — fails-closed today exactly as designed (10 import hits remain pre-implementation).
  - Post-Phase-1 satisfiability: the gate exits 0 when the 10 logical imports are converted to IIFEs (import_hits → 0); the 7 residuals are allowlisted by decision and no longer fail the gate. Residual is intentional and documented, not hidden.

## Fix-3: BLOCKING-003 (Phase 3 ls rc)
- File: `plans/cross-platform-universality-m1/03-phase-entrypoint-optional.md:57-58, 70-71, 125`
- Before: `ls scripts/qoderwork.* 2>/dev/null; echo $?` → claimed `NOT_FOUND (exit 1)`. Reality: Git Bash `ls` on a non-matching glob returns rc=2, so the phase could never read NOT_FOUND/PASS.
- After: replaced with `test -e scripts/qoderwork.sh; echo $?` (rc=0 FOUND / rc=1 NOT_FOUND / rc=2+ UNAVAILABLE) and `compgen -G 'scripts/*.cmd' >/dev/null; echo $?` (same three states; rc=1 NOT_FOUND). The three-state table, the two "For each baseline row" bullets, and §9 rollback verification (L125: `confirm test -e scripts/qoderwork.sh returns rc=1`) were all updated. The `ls` rc=2 gotcha is documented in the prose so future readers do not regress to `ls`.
- git diff: `-| No entrypoint script exists | \`ls scripts/qoderwork.* 2>/dev/null; echo $?\` | NOT_FOUND (exit 1) |` → `+| No entrypoint script exists | \`test -e scripts/qoderwork.sh; echo $?\` | NOT_FOUND (exit 1) |`; `.cmd` row → `compgen -G 'scripts/*.cmd' >/dev/null; echo $?`; rollback line → `confirm \`test -e scripts/qoderwork.sh\` returns rc=1 (NOT_FOUND again)`.
- Verified-by:
  - `ls scripts/qoderwork.* 2>/dev/null; echo $?` → `rc=2`; `ls scripts/*.cmd 2>/dev/null; echo $?` → `rc=2` (confirms the defect).
  - `test -e scripts/qoderwork.sh; echo $?` → `rc=1` (NOT_FOUND PASS); `compgen -G 'scripts/*.cmd' >/dev/null; echo $?` → `rc=1` (NOT_FOUND PASS). `compgen -G 'scripts/*.ts'` → `rc=0` (FOUND) and `test -e scripts/lib/workspace-paths.ts` → `rc=0` (FOUND) confirm the positive branch.

## Fix-4: BLOCKING-004 (Phase 4 Step 5)
- File: `plans/cross-platform-universality-m1/04-phase-ci-and-docs.md:140-151` (Step 5), `:225` (§8 mutation row), `:241` (§10 gate)
- Choice: **Option A** (user implicitly approved "iterate until pass" without scope expansion). Step 5 is removed entirely; the §10 gate item and §8 mutation row are replaced.
- Before: Step 5 ran `capture-state.ts --repository-root /home/zhaoge/workspace/opencode/work-one --output audits/.../pre-change-PHASE-04.json --scope-lock audits/.../scope-lock-PHASE-04.json --phase-id PHASE-04` AFTER Steps 1–4 implementation edits; §10 required "was invoked in Step 5 and produced an audits/ snapshot".
- After: Step 5 becomes "capture-state invocation REMOVED (P-02 Freeze Gate is a pre-implementation ritual)". The text documents the two load-bearing defects (P-02 ordering inversion — capture-state must run BEFORE implementation writes; capture-state.ts L63 throws if the scope-lock file does not exist, and no plan-set step creates or human-approves one). §8 mutation row: "capture-state invoked inside Phase 4 Steps 1–4 → FORBIDDEN (P-02 ordering inversion)". §10 gate: "No capture-state invocation is performed inside this phase's edit steps ... no audit snapshot is required for Phase 4 completion".
- Scope note (documented, not a defect): the task's scope boundary listed creating `scope-lock-PHASE-04.json` as a possible deliverable, but the Fix-4 instruction explicitly chooses Option A ("Remove Step 5 entirely ... no scope-lock") — creating a scope-lock without a human-approval step would reproduce the BLOCKING-004 approval defect. Per user's implicit approval of Option A, the scope-lock is NOT created.
- git diff: the Step 5 heading/paragraph/code-block replaced with the removal rationale + Option-A resolution; §8 row `-| capture-state.ts not invoked or no audits snapshot | P-07 unenforced | Step 5 produces no audit artifact -> blocked |` → `+| capture-state invoked inside Phase 4 Steps 1–4 | FORBIDDEN (P-02 ordering inversion; BLOCKING-004) | ... |`; §10 capture-state invocation row → the "No capture-state invocation is performed inside this phase's edit steps" gate item.
- Verified-by: `test -f audits/cross-platform-universality-m1/scope-lock-PHASE-04.json` → missing (no such file), confirming no dangling artifact is required; `validate-plan.ts` and `validate-phase-progression.ts PHASE-01/02` exit 0 with no reference to the removed invocation; `grep -c capture-state plans/cross-platform-universality-m1/04-phase-ci-and-docs.md` → only the removal-rationale prose remains (no executable invocation). The §10 gate is now satisfiable without any scope-lock/human-approval step inside the plan body.

## Fix-5: MISSED-001 (manifest Status)
- Files: `plans/cross-platform-universality-m1/00-plan-index.md:118`, `plans/cross-platform-universality-m1/03-phase-entrypoint-optional.md:7`
- Before: manifest row `| 3 | PHASE-03 | ... | NONE | BLOCKED-BY-DECISION |`; phase header `**Progression status**: \`BLOCKED-BY-DECISION\``. Illegal per `phase-progression.ts` L5-11 (`NOT_STARTED | IN_PROGRESS | ACCEPTED | BLOCKED | INVALID`) → validator `PHASE_STATUS_INVALID`.
- After: `| 3 | PHASE-03 | ... | NONE | BLOCKED |` and `**Progression status**: \`BLOCKED\``. `BLOCKED-BY-DECISION` remains only as prose (03:1 title, §5/§6/§7/§10 prose, and the DEC-004 decision-ledger Status column) — prose is left untouched per the task.
- git diff: `-| 3 | PHASE-03 | \`03-phase-entrypoint-optional.md\` | NONE | BLOCKED-BY-DECISION |` → `+| 3 | PHASE-03 | \`03-phase-entrypoint-optional.md\` | NONE | BLOCKED |`; `-**Progression status**: \`BLOCKED-BY-DECISION\`` → `+**Progression status**: \`BLOCKED\``.
- Verified-by: `validate-phase-progression.ts plans/cross-platform-universality-m1 PHASE-01` → exit 0, `{"ok": true, "errors": []}`; the `PHASE_STATUS_INVALID` error is gone.

## Fix-6: MISSED-002 (manifest Depends on cell)
- File: `plans/cross-platform-universality-m1/00-plan-index.md:117`
- Before: `| 2 | PHASE-02 | ... | NONE (parallel with 01) | NOT_STARTED |` — validator parses the cell verbatim → `UNKNOWN_PHASE_DEPENDENCY: PHASE-02: NONE (parallel with 01)`.
- After: manifest cell is exactly `NONE`. The "(parallel with 01)" prose moved to a note line under the manifest table (00:121) and to the PHASE-02 file header note (02:10).
- Sub-note (validator-required follow-up, in scope): `validate-phase-progression.ts` also machine-parses the PHASE-02 file header `**Depends on**: NONE (parallel with PHASE-01)` (L4) and reported `PHASE_STARTING_STATE_MISMATCH`. The header is now `**Depends on**: NONE` with the parallelism prose moved to a `Note:` line (02:10). This is a strictly-minimal extension of Fix 6's "move prose off the machine-parsed cell" requirement — without it, the PHASE-02 validator run cannot exit 0.
- git diff: `-| 2 | PHASE-02 | \`02-phase-skill-universalization.md\` | NONE (parallel with 01) | NOT_STARTED |` → `+| 2 | PHASE-02 | \`02-phase-skill-universalization.md\` | NONE | NOT_STARTED |`; `-**Depends on**: NONE (parallel with PHASE-01)` → `+**Depends on**: NONE` + added Note line; plus a one-line note under the manifest table.
- Verified-by: `validate-phase-progression.ts plans/cross-platform-universality-m1 PHASE-02` → exit 0, `{"ok": true, "errors": []}`; `UNKNOWN_PHASE_DEPENDENCY` and `PHASE_STARTING_STATE_MISMATCH` are gone.

## Validator Re-Runs

### validate-plan.ts
- Command: `bun run .agents/skills/deterministic-implementation-planning/scripts/validate-plan.ts plans/cross-platform-universality-m1 C:/Users/USER/ZCodeProject/qoderwork/.worktrees/check-plan`
- Result: `{"ok": true, "mode": "PLAN_SET", "planPath": "plans/cross-platform-universality-m1", "errors": []}` — **EXIT=0**

### validate-phase-progression.ts
- `... plans/cross-platform-universality-m1 PHASE-01` → `{"ok": true, "planPath": "plans/cross-platform-universality-m1", "phaseId": "PHASE-01", "errors": []}` — **EXIT=0**
- `... PHASE-02` → `{"ok": true, "planPath": "plans/cross-platform-universality-m1", "phaseId": "PHASE-02", "errors": []}` — **EXIT=0**
- `... PHASE-03` → **EXIT=1**, `[{"code":"NEXT_PHASE_STATE_INVALID","message":"PHASE-03: expected NOT_STARTED, got BLOCKED"}]` — INHERENT, not a plan defect: this validator's contract is "the NEXT phase must be startable", and a `BLOCKED` phase is by definition not startable. The prior structural failure (`PHASE_STATUS_INVALID`, illegal vocabulary) is gone; manifest and phase header are consistent (no `PHASE_STATUS_MISMATCH`).
- `... PHASE-04` → **EXIT=1**, `[{"code":"PROGRESSION_DEPENDENCY_NOT_ACCEPTED","message":"PHASE-01: status=NOT_STARTED"},{"code":"PROGRESSION_DEPENDENCY_NOT_ACCEPTED","message":"PHASE-02: status=NOT_STARTED"}]` — INHERENT: PHASE-04's dependencies are not ACCEPTED at plan stage (all phases NOT_STARTED pre-implementation). The manifest dependency structure itself is valid (PHASE-01/02 exit 0 proves the rows, cells, and headers parse cleanly).

Honest caveat: the task expected all four phase runs to exit 0; PHASE-03/04 exit 1 for the validator's inherent "start the next phase" semantics, which no plan-text edit can satisfy while the phases are legitimately NOT_STARTED/BLOCKED. These are not well-formedness failures.

## SHA-256 Recomputation

Neither the canonical contract nor the approval decision was modified:
- `canonical-requirements-contract.yaml`: `dd400ed2554d0cfa0bb8b12d1cb4a5b822a0d854102b6c1e74cccc023cd56c58` (unchanged; matches index L11) — NO re-approval trigger.
- `approval-decision.json`: `9212bdf0322d5f01d416762801a14ce02583a0db36225c9e6712e2980372ffe6` (unchanged; matches index L13) — NO re-approval trigger.

## Non-Scope-Creep Statement

- NON-BLOCKING-001..005, MISSED-003/004, DEBT-001/002/003, OUT_OF_SCOPE-001/002: **NOT touched** (by design of the 6-fix frozen package).
- `blueprints/blueprint-cross-platform-universality.md`, `handoff/native-windows-verification.md`, `.agents/skills/**`, `99-final-verification.md`, contract, approval: **NOT modified**.
- `scope-lock-PHASE-04.json`: **NOT created** (Fix-4 Option A removes the Step-5 dependency; creating it would re-introduce the human-approval defect).

## Known Residual (reported honestly)

- **QUALITY-GATES §11 hard limits**: the BLOCKING fixes necessarily ADD content to files that were already at 99.9% of their character limits (DEBT-002 zero-headroom). Post-fix sizes (`[...content].length`): 00=8055/8000, 01=15481/14000, 02=14033/14000, 03=8494/14000, 04=14043/14000, 99=6226/8000. `validate-plan.ts` does not enforce size (exit 0), and DEBT-002 trimming is out of the 6-fix scope, but the hard-limit breach on 00/01/02/04 is real and should be addressed by the main session (trim prose / split) before implementation handoff.

## Self-Check Gate

**Acceptance criteria 自检**:
- [x] Exactly 6 frozen BLOCKING fixes applied (BLOCKING-001..004, MISSED-001/002), each with a Verified-by run.
- [x] Both validators run; `validate-plan.ts` exit 0; `validate-phase-progression.ts` PHASE-01/02 exit 0; PHASE-03/04 exit 1 only for inherent start-semantics (documented).
- [x] Contract SHA-256 and approval SHA-256 unchanged — no re-approval trigger.
- [x] No NON-BLOCKING / DEBT / OUT_OF_SCOPE fix applied; blueprint and handoff untouched.
- [x] Report written to `audits/cross-platform-universality-m1/2026-08-03-fix-m3.md` with Verified-by lines for all 6 fixes.

**Evidence checks 自检**:
- [x] Fix-1 regex → exactly 6 files (rc=0); the broken form returns 0 (defect reproduced by reviewers G1/G2).
- [x] Fix-2 step-2 scan → `import_hits=10 residual_hits=7` with the exact 7-item allowlist, rc=1 pre-implementation (fails closed; satisfiable post-fix).
- [x] Fix-3 probes → `test -e` rc=1 / `compgen -G` rc=1 (NOT_FOUND); `ls` glob confirmed rc=2.
- [x] Fix-4 → no capture-state invocation remains executable in Phase 4; §10 gate and §8 mutation row consistent; no dangling scope-lock artifact.
- [x] Fix-5 → `PHASE_STATUS_INVALID` gone (manifest `BLOCKED` + header `BLOCKED`).
- [x] Fix-6 → `UNKNOWN_PHASE_DEPENDENCY` and `PHASE_STARTING_STATE_MISMATCH` gone.
- [x] `git diff` excerpts captured for every Edit (all hunks above).
- [x] Contract/approval untouched (git diff empty) and SHA-256s re-computed.

**Self-Decision**: 待主会话复核
- Escalation notes for the main session: (1) PHASE-03/04 `validate-phase-progression.ts` exit 1 for inherent start-semantics — expected, not defects; (2) QUALITY-GATES hard-limit breach on 00/01/02/04 introduced by the frozen fixes — needs prose trimming / split before implementation; (3) `scope-lock-PHASE-04.json` deliberately NOT created per Fix-4 Option A.

— M3 (general-purpose), 2026-08-03. Self-Check Gate only; no Final Gate / Accept signed.
