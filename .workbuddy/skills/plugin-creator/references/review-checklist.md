# Plugin Review Checklist

- Plugin scope is role-oriented rather than a single isolated task
- Display names and user-facing text match the user's language
- Required skills exist and their local links resolve
- Connector claims match actual configured capabilities
- Example flows are consistent with the plugin's target role
- No placeholder files or stale references remain
