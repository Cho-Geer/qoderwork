# 2026-07-01

## 阅读 documents/ 全量文档

- 完整通读了 documents/ 目录下全部 8 个文档，建立对 OpenCode 框架的系统理解
- 核心文档：opencode-cognitive-map.md（全景架构）、opencode-framework-comprehensive-understanding.md（深度理解）、framework-deep-reference.md（Plugin 执行流）
- 参考文档：opencode-framework-quick-reference.md、opencode-tool-reference.md、session-concepts-complete.md、opencode-cli-acp-integration.md
- 关键发现：框架非 fork OpenCode 源码，而是完全通过其原生扩展机制（Plugin/Agent/Tool/Skill/Rule）构建了四层十角色的多智能体编排系统

## 生成 11 子系统深度分析报告

- 深度探索 .opencode/ 源码（469+ 文件），并行分析全部 11 个子系统
- 生成 `documents/opencode-subsystems-report.md`：MVC架构、DB-canonical、Permission Matrix、Concurrency Safety、Hardened Enforcement、Framework Harness、Multi-Agent、Log Central、DB Management、Templatization、TS+Bun Runtime
- 关键源码文件覆盖率：service/dispatch/*（19文件）、service/gate/*（30+文件）、service/permission/*（3文件）、service/session/*（9文件）、plugin-handlers/*（30文件）、lib/*（60+文件）
- 核心发现：Before=阻断/After=副作用基础不变式、Deny-First权限语义、DB as Single Source of Truth、anti-bypass FIRST解决throw-preemption bug、Token-based Gate Lock（Agent无法自清除）、累积不重置计数器

## 更新 documents/ 文档与实际代码对齐

- 验证 documents/ 下文档与实际代码的一致性，发现多处数值不准确
- 修正 opencode-cognitive-map.md：
  - Plugin 数统一为 29 个（之前文档自相矛盾：25/26/29 混用）
  - 自定义工具数 20 → 23（新增 read_skill、skill_read_attest、rule_read_attest）
  - Lib 数 49 → 50
  - 更新生成日期为 2026-07-01
- 修正 opencode-tool-reference.md：
  - 自定义工具数 20 → 23
  - 新增 Section 3.5「Read Attestation 工具（3 个）」
  - 更新生成日期为 2026-07-01
- 更新 INDEX.md：添加最近更新说明（2026-07-01）
- 验证方法：通过 Agent 子代理深度探索 .opencode/ 源码结构（304个.ts文件，排除 node_modules）

## 深入调查与全面文档更新（第二轮）

- 系统核对 documents/ 下全部 8 个文档与实际代码的一致性，发现并修正多处不准确
- 修正 opencode-subsystems-report.md：
  - "16+ Plugin" → "29 个 Plugin handler"
  - Before handler 计数 13 → 15，After handler 计数 13 → 14（与 plugin_execution_order 一致）
- 修正 opencode-framework-comprehensive-understanding.md：
  - 架构概览表：36 Plugin → 29 Plugin handler，22 Tool → 23 Tool，28 Shared Lib → 50 Lib
- 修正 opencode-framework-quick-reference.md：
  - 表 B 文件路径从错误的 `plugins/*.ts` 修正为 `plugin-handlers/*/*.ts`
  - 表 B 标题和说明更新，注明完整 handler 见 cognitive-map.md 附录 C
  - 表 C 添加 3 个新工具（read_skill、skill_read_attest、rule_read_attest）
- 修正 framework-deep-reference.md：
  - Section 1 标题 "26 文件" → "29 个 Handler"
  - 添加 Dispatcher 架构说明（5个 Plugin 文件派发到 29 个 Handler 模块）
  - Before/After Hook 文件路径全部修正为 `plugin-handlers/*/*.ts` 格式
  - 更新文档日期为 2026-07-01
- 修正 session-concepts-complete.md：
  - 文件路径修正：`plugins/gate-before.ts` → `plugin-handlers/before/gate.ts`
  - `plugins/task-before.ts` → `plugin-handlers/before/dispatch.ts`
  - `plugins/task-after.ts` → `plugin-handlers/after/dispatch.ts`
  - 表 7 中 `task-after.ts` → `plugin-handlers/after/dispatch.ts`
  - 更新版本号为 v1.1.0，添加更新日期
- 更新 INDEX.md：
  - "最近更新"说明扩展为完整更新列表（7个文档）
  - 各文档摘要中的计数全部更新为准确值

## 关键发现：Dispatcher 架构

- 框架使用 Dispatcher 模式：5个 Plugin 文件（plugins/*.ts）注册 hook 事件，派发到 29 个 Handler 模块（plugin-handlers/*/*.ts）
- 这是第一轮文档生成时的架构，但部分文档（framework-deep-reference.md）未正确描述此架构
- 所有 before/after handler 文件实际位于 `plugin-handlers/before/` 和 `plugin-handlers/after/` 目录

## 生成 Enforcement & 豁免机制完整矩阵

- 通过 3 个并行 Agent 深度分析 .opencode/ 源码
- Agent 1：分析 enforcement 模式体系（advisory/strict/locked）
- Agent 2：映射所有工具的 before/after hook 链（23 个自定义工具 + 11 个内置工具）
- Agent 3：分析工具豁免机制（Agent 级、路径级、工具级豁免）
- 生成 `documents/opencode-enforcement-exemption-matrix.md`（~780 行）
- 涵盖内容：
  - 三种 enforcement 模式定义、切换规则、优先级解析
  - Before Hook 链 14 个 handler 详解（执行顺序、阻断行为）
  - After Hook 链 14 个 handler 详解（fire-and-forget、不阻断）
  - 23 个自定义工具完整 hook 链矩阵
  - 按 enforcement check 分类的豁免机制矩阵
  - CodeGraph/Scope/UC7KS/Dispatch/Git Hook/Checklist/Safe Shell 豁免详情
  - Agent 权限级别矩阵（7 个 Agent）
  - 阻断/警告行为速查表
  - Guidance Gate 两阶段协议
- 关键发现：
  - anti-bypass FIRST 执行防止抢占
  - read-only 工具不累积失败计数
  - Super-Admin break-glass 需 `[BYPASS <incident_id>]` 标记
  - DAG-exempt agents: Meta-Planner, Orchestrator, Super-Admin, Knowledge-Curator
  - CodeGraph 豁免路径：`.task_temp/`, `docs/`, `.opencode/agents/*.md`, `.understand-anything/`, `.codegraph/`

