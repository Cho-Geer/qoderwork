# 2026-07-13

## work-one before 链验证：tool-governance 对 `safe_shell gh issue create` 的命中顺序

**任务**: 用 /pre-flight-enforcement 确认 `safe_shell gh issue create --repo ...` 在真实 active before 链上先命中 tool-governance（而非仅 unit/component 测试）。

**结论**: ✅ 已确认。真实 `before-dispatcher` 执行序列 = guidance-bridge → permission-safety → behavioral-path-guard → scope → **tool-governance**(HANDLER-BLOCK [REPO-OP]) → 链短路；path-validate/codegraph 未执行。tool-governance 为首个且唯一 denier。

**关键事实/可复用方法**:
- `before` 链是单一 `tool.execute.before` hook（`plugins/before-dispatcher.ts`），内部按 `project.config.json` 的 `plugin_execution_order.before`（与 DEFAULT_ORDER 一致）串行调度，首个 throw 短路。tool-governance 位于 index 6。
- **live 验证真实 dispatcher 的方法**：`bun x esbuild before-dispatcher.ts --bundle --format=esm --platform=node --external:bun:sqlite` 后 `bun run` 打包产物。绕开 `bun run` 直接加载报 `export 'parseShellWriteTargets' not found`（tool-scope-paths ↔ shell-targets 循环 re-export，bun 原生 ESM 链接失败；仅 serve 的打包器能解析）。
- **测试只停留在 unit/component 的根因**：上述循环 re-export 导致 `before-dispatcher` 无法被 `bun run` 独立加载，故现有测试只能直接 `handle()` 单个 handler，无法测排序。
- 日志落点：`writeLog` 写 JSONL 到 `<root>/.task_temp/_logs/<date>/`（非 `.opencode` 内）；HANDLER-START/HANDLER-BLOCK 在 `plugin-before-dispatcher-runtime.log`，deny 汇总在 `audit.jsonl`。
- 安全：tool-governance 在工具执行前拒绝，gh 未实际运行，未创建真实 issue。

**产物**: `qoderwork/tool-governance-before-chain-verification.md`（验证报告）

## 修正：前次报告未真正走 serve-api；本次补做严格 serve-api 验证

**修正点**: 前次 pre-flight 计划列了 serve-api 但执行用 esbuild 直调 `before-dispatcher` 替代，**未真正用 serve-api**。用户质疑后，本次严格按 serve-api skill §0 Pre-Flight + 核心操作执行，结论不变且被生产路径佐证。

**serve-api 实操关键事实**:
- **§0 F2 SSE daemon 启动坑**: `wsl ... bash -c "setsid bun ... &"` 内联 `&`/setsid 触发 WorkBuddy safe-bin 翻译错误（"Failed to translate safe-bin"）。改用 **Bash 工具 run_in_background** 启动 `bun run sse-daemon.ts` 成功（日志 `SSE connected`）。
- **agent 选择决定能否到达 before 链（关键）**: `opencode.json` 中 build/general/Orchestrator 的 `safe_shell` 是**匹配对象**（仅放行 npm run*/git*/cat* 等），`gh issue create` 不匹配 → **原生层 deny，before 链不执行**；只有 **explore** 是扁平 `"allow"` → 原生放行 → before 链才运行、tool-governance 才拦截。
- **前次"serve-api 无法隔离 before 链"假设不成立的前提**：选对 agent（explore）后 serve-api 是有效且更强的验证路径。
- serve-api 证据：HANDLER-START 序列含 `behavioral-path-guard` 的那组 = guidance-bridge→permission-safety→behavioral-path-guard→scope→tool-governance，随后短路；`audit.jsonl` 记 `governance_block ruleId=REPO-OP layer=repo-policy outcome=deny agent=explore`。与 esbuild 直调结果完全一致。
- 清理：两条测试 session 已 `POST /session/{SID}/abort` 清理；`GET /question` 终态 `[]`（F4 合规）。

**产物**: `qoderwork/serve-api-before-chain-verification.md`（serve-api 验证报告，含对前次的修正）

## work-one blueprint v3.0.0 Phase 8/9 实施（tool-governance-mvc-refactor）

**任务**: 按 /pre-flight-enforcement 实施 blueprint 的 P0 必须项 Phase 8（3 bug）+ Phase 9（3 安全绕过）。

**关键发现**:
- work-one 工作树已有 36 个未提交文件变更（Phase 0-7 主体，560+/362-）— 前序 session 已完成 Phase 0-7，Phase 8/9 才是真正未做项。蓝图 snippet 行号已漂移，必须读真实源再改。
- 本次实际新增/修改：8.1 isExemptPath 追加非源码豁免(.gitignore/package.json/tsconfig.json 等)；8.3 DEFAULT_ORDER + project.config.json 重排(tool-governance 前置到 permission-safety 之后、behavioral-path-guard 之前)；8.4 向 codegraph.test.ts 追加 3 个 exempt 测试；9.1 opencode.json 删 node -e/ts/js allow 改 deny + Orchestrator.md 描述修正；9.2 shell-guard 删 isOrchestrator 死代码 + allow-write 后门(并删孤儿 normalize import)；9.3 tool-scope-match.ts + shell-config.ts 正则加固 bracket notation/字符串拼接；9.4 新建 write-bypass-prevention.test.ts。8.2 前序已修复。

**蓝图错误修正**: 蓝图 8.4 用 codegraph.handle(...)，但 codegraph.test.ts 实际 import { handle } -> 已改用 handle(...)，否则 ReferenceError。

**编译坑**: 删 isOrchestrator 后 normalize import 变孤儿(line 17)，必须同步删除否则 TS noUnusedLocals 失败。

**可复用方法**: WSL 文件编辑用 Python 脚本写 Windows temp 再 wsl python3 /mnt/c/Users/USER/AppData/Local/Temp/*.py 执行，避开 UNC Write/Edit 拒绝与 bash 引号地狱。

**验证状态(全绿)**: codegraph.test.ts 9/9 ✅；write-bypass 5/5 ✅；tool-governance 全量 41/0 ✅；tool-governance-handler 5/0 ✅；safe-bash 6/0 ✅；before-dispatcher import smoke ✅；grep 负向 isOrchestrator=0 / allow-write=0 ✅；node deny=3(`node -e`/`node *.js`/`node *.ts` 三处皆 deny，初估 1 实为 3) ✅。§5.3 live E2E 未跑，标 PARTIAL（按蓝图 rule #5 不得宣称 COMPLETE）。蓝图 checkbox/status 未改（仅 reviewer 可改）。

**Phase 2 audit 结论**: 8.1 配置层修正(14 非源码模式同时写入 project.config.json enforcement_exemptions.codegraph.exempt_path_patterns + codegraph.ts 硬编码 fallback)为执行中发现的蓝图偏差——因 exemptions.ts getCodeGraphExemptPatterns() 恒非空、isExemptPath 在配置模式处提前 return，仅改硬编码 fallback 不生效，故 8.1 必须在配置层落地才生效。DEV 项。

## work-one 剩余未实施清单复核（debug-environment-toolkit 纪律）

**方法**: 不信任蓝图 checkbox（弱模型禁勾），交叉比对蓝图 §5.1-5.5/§7 `[ ]` 与 git status(46 文件) + 源码阅读。

**关键发现（验证过）**:
- `plugin-handlers/before/scope.ts` handler **已薄**（仅调 validateWriteScope + throw，行 8-13）→ §5.2 1337 adapter 迁薄实质已完成，剩回归验证非代码。
- **唯一未动的代码文件**: `service/gate/scope-validate.ts`（validateWriteScope 单体 ~200 行，8 职责）与 before 链统一日志。git status 证实二者从未改。
- `validateWriteScope` 拆分 seam：modify-gate@43-50 / path-scope+unparseable-shell@61-102 / backup-bypass@104-123 / per-path@125（ROUTE-MISMATCH@131, UC7KS@177-178）。
- **日志缺口（已验证）**: 20 个 before handler 全调 writeLog，但大小写敏感搜 `ruleId|layer=|outcome=` **仅 anti-bypass.ts 命中**。其余缺标准三字段。模板 anti-bypass.ts:120-128。

**分类**: A 开放代码=scope-validate 拆分(§7 1449)+before 链统一日志(§5.2 1344/§7 1453)+复合命令迁移(§5.2 1347,大型); B live E2E=§5.3 全部+§7 1459/1460(reviewer serve-api); C reviewer 审计=§5.4/§5.5/§7 1455/1456; D 代码已完成待勾=8.1-9.4 全部(工作树已落地测试通过)。

**产物**: `logs/2026-07-13-tool-governance-remaining-backlog.md`（含 WSL 手动编辑方法）。

## A1 详解：scope-validate.ts 拆分（逐行 concern 映射，本轮回读全文件验证）

**公开面**: `validateWriteScope` 在 scope-validate.ts:34 定义，`service/gate/index.ts:182` re-export，唯一运行时消费者是 `plugin-handlers/before/scope.ts`（行 1-13 仅调用+throw）。无其它引用 → 拆分不波及。
**测试缺口**: `service/gate/` 下无 `__tests__`，需新建 `service/gate/__tests__/scope-validate.test.ts`（仿 `service/tool-governance/__tests__` mock 风格）。

**validateWriteScope 单体职责拆解（行号=本轮回读确认）**:
- 预过滤: modify-tool gate@43-50（非 modify 工具→allow 终态）; filePath@52-59（无路径→allow 终态）
- scopeResult 计算@61-62（applyPathScope + paths）。蓝图"write-permission"实为该 gate，非独立检查。
- C-shell(unparseable)@65-102: **该块必为 terminal**（read_only@71-78→allow 终态; trusted script@84-88→allow 终态; 否则@89-101→block backup-bypass）。拆时不要"优化"成 continue。
- C-backup-bypass@104-123: safe_shell/bash + 有写目标 → block backup-bypass（shouldBlock 为 false 时 fall-through 非 return）。
- per-path 循环@125-210（taskId/domainId 循环外算一次@127-128）: ROUTE-MISMATCH@131-146(WARN 审计,**非阻塞**); AGENT-UNRESOLVED@149-155(WARN 审计); CONFIG-READ attest@157-174(WARN 审计); UC7-001@176-190(block uc7ks-tracking); UC7-005 size cap@192-208(block write-scope-violation)。

**必须保留的语义（拆分即回归测试断言）**:
- 三个审计项(ROUTE-MISMATCH/AGENT-UNRESOLVED/CONFIG-READ)保持 WARN 审计、**绝不转 blocking**。
- 每条 block message 末尾 `[STOP]...[REPORT]...` 字符串逐字保留（enforcement 契约）。
- **每个 `return {blocked:true}` 必须包在 `if (shouldBlock("rule"))` 内**，不得裸 return——否则跳过软 enforcement 降级通道。
- 循环内 UC7-001 先于 UC7-005 检查，顺序保留。
