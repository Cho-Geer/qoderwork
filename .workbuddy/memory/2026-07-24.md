# 2026-07-24 工作记录

- 在 Ardot 画布上完成 task-lens 模块调用关系图（带箭头的连线和模块卡片）。
- 关键发现：Ardot 的 SVG 导入不渲染 `<line>` 和 `<marker>`，需改用 `<path>` 画线、`<polygon>` 显式绘制箭头。
- 生成了复用脚本 `gen_module_graph.py` 用于后续更新 call graph。
