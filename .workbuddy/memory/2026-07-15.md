# G5 脚本证据复核交接 + Session 就绪核验

**为什么**: 用户交付 PT-WM-00R2 G5 审核结论，要求理解内容并让 session 准备就绪。审核核心：G2–G4 不是"测试已完成"，而是 12 个 DRY_RUN 计划脚本已落盘。

**核验结果（已逐项确认属实）**:
- `blueprints/blueprint-permission-template-driven-enforcement.md` 已升级 **v1.6.0**，含 §1.8 G5 复核；`e2e/permission-template-enforcement-test-spec.md` 已升级 **v1.5.0**。
- 12 个 G2–G4 测试脚本落盘：`scripts/_b_pt_wm_00r2_g{2,3,4}_*.ts`（g2:T-PT-039/040/041/042；g3:T-PT-048/049/050/051/052；g4:ADV-PT-008/009/010），共 4551 行；另有 2 个 live 辅助脚本（live.ts / live_e2e.ts）共 4931 行。
- 12 个 evidence 目录均存在：`e2e-evidence/L3/permission-template-enforcement/rework-pt-wm-00r2/{g2-t039..g2-t042,g3-t048..g3-t052,g4-adv008..g4-adv010}/`，各含 `*-evidence.json` + `*-checklist.md`。全部 `dryRun:true`。
- 全部 evidence 均为 dryRun，不能将任何 test ID 升级为 PASS。
- **T-PT-048**：仅 4 个静态 fixture-shape 断言为 true；真实 state-validator、DB/session-map、mismatch oracle 未执行 → 仍 NOT-RUN。
- **T-PT-042 runner 偏差（已用 grep 确认）**：`_b_pt_wm_00r2_g2_t042.ts` 把 `list`、`skill_read_state` 列为未认证 allowlist，遗漏源码权威集合 `config_read_attest`、`rule_read_attest`。权威集合（skill-policy.ts）= read/glob/grep/question/skill/config_read_attest/skill_read_attest/rule_read_attest（8 个）。runner artifact = **INVALID**；测试需求 T-PT-042 仍 BLOCKED。
- **4097 端口**：2026-07-15 审核时不可连接 → 历史 question（QID `que_f613576b9001D3ig6qA8KxMCMU`）当前 pending/closed 状态 **UNVERIFIED**，不得写作"仍挂起"。

**下一步序列（已登记为任务 #1–#4）**:
1. 修正 T-PT-042 runner 固定 allowlist（移除 list/skill_read_state，补 config_read_attest/rule_read_attest）。
2. 重跑其 DRY_RUN artifact。
3. Reviewer 启动隔离 serve：`FRAMEWORK_SKILL_READ_HARD_GATE=1`（T-PT-051 额外 DRY_RUN=false + H2_AUTHORIZED=true）—— 需用户侧环境操作。
4. 真跑 T-PT-048/049/050 前置 → T-PT-039–042、T-PT-051/052、ADV-PT-008–010。

**决策**: 不修改 work-one 源码；不改变 BLOCK-PT-02/03；不将任何 G2–G4 test ID 改为 PASS。green/dispatcher 集成不得替代 fault、20 轮 concurrency、mutation score、真实 executor-boundary、target-state evidence。

**环境注意**: Bash 工具 cwd 为 `//wsl.localhost/Ubuntu-24.04/home/zhaoge/workspace/qoderwork`；`cd /home/zhaoge/workspace/qoderwork` 会落到 Git Bash 同名异目录（仅含 .workbuddy/scripts/team-elevation）。后续文件操作须用 `//wsl.localhost/...` 全路径或保持默认 cwd。

## 执行记录（2026-07-15 下午，pre-flight-enforcement 流程）

- 任务 #1（修正 T-PT-042 runner）+ #3（重跑 DRY_RUN artifact）已完成。
- 依据 work-one 源码 `.opencode/plugin-handlers/before/skill-policy.ts` 第 20–32 行 `PRE_ATTEST_ALLOWLIST` 校验权威 8 项集合，确认 runner 原偏差。
- 编辑 `scripts/_b_pt_wm_00r2_g2_t042.ts`：移除 `list`/`skill_read_state`、补入 `config_read_attest`/`rule_read_attest`；`bun` 默认 `DRY_RUN=true` 重跑重新生成 `t042-evidence.json`/`t042-checklist.md`（artifact 含 `dryRun:true`，allowlist 与源码一致；`tool=list`/`tool=skill_read_state` 计数均为 0）。
- 同步更新 `blueprints/...enforcement.md` §1.8 与 `e2e/...test-spec.md` §7.1 G5 注：runner 不再 INVALID，T-PT-042 仍 BLOCKED。
- 日志：`logs/2026-07-15-pt-wm-00r2-t042-runner-fix.md`。
- 未执行：#2（隔离 serve，需用户 FRAMEWORK_SKILL_READ_HARD_GATE=1）与 #4（真跑）。dry-run 不改变任何 test ID 裁决。

## 隔离 serve 启动程序确认（2026-07-15，回应"确认如何启动隔离serve"）

- **权威入口脚本**：`scripts/_b_pt_wm_00r2_live.ts`（专用隔离 serve launcher，非 serve-api 消费者）。由 reviewer 在 WSL 内运行：`wsl -d Ubuntu-24.04 bash -c "export PATH='.../home/zhaoge/.bun/bin' && cd /home/zhaoge/workspace/qoderwork/scripts && bun run _b_pt_wm_00r2_live.ts"`（Bash 工具须 `run_in_background:true` 持久化 serve）。停止：`... bun run _b_pt_wm_00r2_live.ts --stop`。
- **launcher 自动完成项**：ISOLATED_PORT=4097（主 serve 4096 不受影响）；`mkdtempSync` 每次新建 `ISOLATED_HOME=/tmp/pt-wm-00r2-live-XXXX` → 自动满足"新 DB"契约；`FRAMEWORK_DB_PATH=ISOLATED_HOME/framework-state.db`；`FRAMEWORK_SKILL_READ_HARD_GATE=1`；`OPENCODE_LOG_DIR` 隔离；3s 后 `GET /session` 健康检查。
- **launcher 不做、需 reviewer 手动补齐（测试式样书 §8.1 第1步）**：① 创建隔离 worktree；② 创建 root/child session；③ 发放精确 allowed-path grant（bootstrap grant，仅覆盖 00R2 allowed paths，用后消费）。缺任一项 → 结果 INVALID，弱模型不得改码。此外 launcher **不清 Bun cache**，启动前须 `rm -rf ~/.bun/install/cache`（blueprint §1.8 明确要求"清 Bun cache、重启隔离 serve"）。
- **⚠️ worktree 与 launcher 的关键关系（用户追问澄清）**：launcher 只隔离"运行时状态"（端口/DB文件/环境变量/日志），**完全未隔离"源码树层"**——`WORK_ONE` 硬编码主树、`cwd: WORK_ONE`（`_b_pt_wm_00r2_live.ts:22,71-72`），既不创建也不指向 git worktree。§8.1 的"隔离 worktree"是源码树层隔离，缺它则：① 真实写文件（T-PT-051 child safe_edit 等 DRY_RUN=false）污染主树；② `attestSkillRead()` 从 worktree config 解析 required list（blueprint L318），无法锚定被测基线；③ 测试式样书 L38 硬性要求"新 commit/worktree identity"，无则整批 INVALID。更深：launcher 连承载 worktree 的能力都没有（不读入 worktree 路径、不接受参数），故"按之前命令启动 launcher"≠满足 §8.1；须 `git worktree add` + 让 launcher 指向该 worktree（改 WORK_ONE 或加 env override 如 PTWM_WORKTREE）。三者（worktree=git/fs 层、session=运行时层、grant=DB 授权层）是不同轴，launcher 均未自动化。
- **端口一致性**：main serve=4096（`start-serve.ts` 默认），isolated=4097（本 launcher + 全部 `_b_pt_wm_00r2_*.ts` 脚本 `SERVE_PORT` 默认 4097）。G5 审核"4097 不可连接"即因隔离 serve 未起。
- **⚠️ H2_AUTHORIZED 缺口（RISK/UNVERIFIED）**：blueprint §1.8 第103行称 T-PT-051 额外需 `DRY_RUN=false + H2_AUTHORIZED=true + FRAMEWORK_SKILL_READ_HARD_GATE=1` 三重条件，测试式样书 §8 也写"reviewer 启动隔离 serve + H2 授权"。但：(a) `grep -r H2_AUTHORIZED` 在 `work-one/.opencode` 全树**无匹配**（FRAMEWORK_DB_PATH / FRAMEWORK_SKILL_READ_HARD_GATE 有消费者，H2 无任何消费者）；(b) `qoderwork/scripts` 也无消费者；(c) `live.ts` launcher **未注入** H2_AUTHORIZED。结论：H2_AUTHORIZED 当前在代码层无 enforcement 落点，是式样书/blueprint 的文档债务。reviewer 跑 T-PT-051 前须先确认：H2_AUTHORIZED 是否已废弃（仅 FRAMEWORK_SKILL_READ_HARD_GATE 生效），还是必须在 launcher 环境 `export H2_AUTHORIZED=true` 前补齐对应消费者——否则 T-PT-051 的 H2 前置无法被任何代码验证。
- **结论**：本 session 不代启隔离 serve。#2 是 reviewer 侧环境操作，且 §8.1 的 worktree+session+grant 三项前提尚未具备；4097 必须由 reviewer 按上述程序在 WSL 内拉起。
