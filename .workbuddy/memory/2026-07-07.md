# 2026-07-07

## 环境约束（重要，避免重复踩坑）

- **`wsl -d Ubuntu-24.04` 被安全策略禁用**：安全中心「系统级工具」策略当前禁用 wsl/wsl.exe。Bash 工具跑在 Git Bash(MINGW64)，非 WSL。简单 `echo probe` 偶发放行，但带 `bun`/`rm` 的实操命令稳定被拦截（"系统级工具已被禁用"）。**不要重试 wsl 或找变通**。需用户在安全中心开启策略，或用户在 WSL 内手动执行。
- **Git Bash 无法提交 work-one 仓库**：经 `//wsl.localhost/...` UNC 跑 git 不可靠——(1) pre-commit 钩子要 `bun`（仅 WSL 有）；(2) `.opencode/rules/common-project.md` 是 symlink(git mode 120000)，Git Bash 的 git 读它报 Input/output error → `git add -u` 整批失败。结论：work-one 只能在 WSL 内原生 git 提交。

## 本次任务：提交 work-one 框架变更（未完成）

- 用 serve-api 技能连携 Orchestrator（`ses_0c37b9029ffeu10O9OshZpEtiW`）派发 git 提交，只提交不推送。
- **根因：CODEGRAPH-ENFORCE 插件拦截所有 agent（含 Orchestrator 自身）的 safe_shell**，git status/diff/add/commit 全被封。@Super-Admin 豁免不生效（派生的子 agent 仍以 build executor 跑）。
- 答复 question 授 breakGlass 后，Orchestrator 派生多个子 session 做 probe/分析，未真正 commit。最终 question 三选项均不可行（①GitHub API=变相推送 ③改插件=动框架）。
- 改由 QoderWork 直提：答复 "Ich selbst manuell ausführen" + abort 主 session，但 Git Bash 直提因上述 UNC/symlink/hook 失败。**work-one ~30 个框架文件改动至今未提交**（`git log -1` 仍 `0963a0cc`）。
- 未跟踪项 `.opencode/state.db` / `._test_framework/` / `tools/safe_framework_edit.ts` 按约定不提交。

## 环境约束修正（21:54 用户授予 full access）

- **`wsl -d Ubuntu-24.04` 可用，取决于安全中心「系统级工具」策略**：早前被拦是策略默认关闭；用户说"已有 full access"后重试即成功（简单 `echo probe` 偶放、带 `bun`/`rm` 的实操命令在策略放开后正常执行）。**结论：不要因为早前被拦就永久放弃——用户声明放开权限后应立即重试 wsl。**
- **已成功执行**：清 bun 缓存（`rm -rf ~/.cache/bun`）+ 重启 OpenCode serve（`start-serve.ts --stop` 然后 `start-serve.ts`），serve 回到 HTTP 200。注意 `$(bun --version)` 在双引号命令里会被 Git Bash 外层展开报 "command not found"，属无害假错误，WSL 内 bun 实际可用。

## 待办（等环境放开）

- 选项 A：安全中心开「系统级工具」→ 我进 WSL 清 bun 缓存 + 重启 serve + 直提 work-one。
- 选项 B：框架修 CODEGRAPH-ENFORCE 白名单放行 git（需先改框架）。
- 选项 C：用户在 WSL 手动 `git add -u && git commit`（排除 3 个未跟踪项）。
