# 2026-07-14 — permission-template enforcement Phase 0 (PT-WM-00) gate work

- 按 blueprint-permission-template-driven-enforcement.md + 测试式样书 启动 Phase 0 受控交付准备 (PT-WM-00)。
- V1: 两份测试 Skill (requirements-to-test-specification, test-specification-execution) 从 Windows 用户级 `C:\Users\USER\.workbuddy\skills\` 原样同步到 `work-one/.opencode/skills/`，逐文件 SHA-256 一致 (R2TS_IDENTICAL / TSE_IDENTICAL)。runtime 文件哈希: r2ts SKILL.md=6c2eb6c2, TEST-SPEC-TEMPLATE=cb3f9151; tse EXECUTION-REPORT=7a2751c7, SKILL=d6fbddcc。
- V2: `opencode debug skill` (binary 在 /home/zhaoge/.opencode/bin/opencode，不在 PATH) 已发现两个 Skill，输出存 `qoderwork/e2e-evidence/L3/permission-template-enforcement/skill-discovery.txt` (line 75 + 105)。→ BLOCK-PT-01 (部署+SHA+发现轴) 解除。
- T-PT-002 negative: /tmp 篡改副本 SHA 与 source 不一致 → gate 拒绝 (ORA-PT-01 负向 oracle 通过)。
- V3 (REQ-PT-002 / T-PT-003/004) 机制查清: required_skill_reads 当前仅 [preflight-lite, codegraph-first] (project.config.json:346)；attestSkillRead 走 read_audit SQLite (verifyRead) + template_resolution.required_skill_reads。本回合未改 project.config.json（framework-path；phase0-enforce 会按这些 reads 拦截活跃 session，需配合 controlled session 一起做），未驱动 live session attest。V3 live attest 是下一步受控任务。
- 未进入 Phase 1 代码修改 (PT-WM-01/02): BLOCK-PT-01 解除但 REQ-PT-002 attest 门未过，且 blueprint 禁止改 checkbox/status。
- 环境事实: wsl bash -c 内变量赋值 ($X=...) 与 `$(...)` 内带引号路径会被剥离/出错 → 用字面路径 + `cd ... && sha256sum file > out` 重定向模式；heredoc 含反引号会被 bash -c wrapper 吞掉 → 写文件改用 Write 工具。opencode 二进制 /home/zhaoge/.opencode/bin/opencode；serve 已在 4096 运行 (PID 418455)。

## T-PT-003/004 集成执行（隔离 harness，本回合完成）
- **方法确立**：`attestSkillRead({agent,sessionID,worktree,taskId})` 可用隔离 harness 验证——`getDb()` 对 `FRAMEWORK_DB_PATH` 空文件自动建全量 schema（read_audit@654/substate_kv@312/gate 表）；`OPENCODE_ROOT`=temp worktree 控 config；绝对路径 import 真实实现，无 mock。harness 存 `/tmp/pt-harness-t003-t004.ts`（源在 Windows temp）。
- **关键陷阱**：`attestSkillRead` 内 `verifyRead(agent,filePath)` **不带 sessionId**，按 agent+path+5min 窗跨 session 匹配读记录。故 happy/negative 两例须用**不同 agent**（build / explore）隔离读历史，否则 negative 会误判 verified:true。播种 `recordRead` 须 contentLength=fileSize 且 file_hash=当前 SHA-256（verifyRead 查 full-read≥0.8 + hash 匹配）。
- **T-PT-003 = PASS**：build 读全 2 skill → verified:true，files_verified=2 一一对应（ORA-PT-02）。
- **T-PT-004 = FAIL（first-failure，写阻断半部）**：explore 缺读 1 → verified:false+unread_files（认证契约半部 PASS）；但"写工具计数=0"半部**当前 runtime 不强制**——`rule-disposition.ts:38 checklist-incomplete=audit_only`、phase0-enforce=RETIRED、checklist-validate=LEGACY（gating 改 rule-disposition 驱动）、skill-policy 对写工具仅 WARN 不 throw、safe_edit/safe_shell/safe_mkdir 在 checklist_passthrough_tools。
- **待 reviewer 裁决**：FAIL 系"式样书期望（REQ-PT-002 失败即写零执行）"vs"活跃 runtime 刻意 audit-only 设计（skill-policy 头注 only warn）"的张力——要么属 Blueprint P1 未实现的强制项，要么应校准式样书 ORA-PT-02。按 §8.1，T-PT-004 未过 → 不得进入 T-PT-006+ 或任何代码修改。
- 产物：`e2e-evidence/L3/permission-template-enforcement/` 下 `execution-report-t003-t004.md`、`t003-t004-attest-harness.json`、`t004-write-block-static-evidence.txt`。
