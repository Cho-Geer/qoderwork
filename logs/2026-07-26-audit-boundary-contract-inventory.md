# 2026-07-26 审计边界与 Plan 契约更新清单

## Context
依据 `handoff/2026-07-26-audit-boundary-and-plan-contract.md`，整理需求边界、Plan、测试与审计的更新面。

  ### TASK

  - [REQ] 依据 handoff/2026-07-26-audit-boundary-and-plan-contract.md 整理需更新的全局规则与 skills。
  - [SCOPE] AGENTS.md、RULES.md、相关 .agents/skills/；本轮只读，不改文件。
  - [MODE] SINGLE

  ### PLAN

  - [S1][DONE] 对照 handoff 与现有规则、模板、校验器职责。
  - [S2][DONE] 划分全局原则、Plan 强制契约、测试/审计消费规则。

  ### EVIDENCE

  - [E1][VERIFIED] 现有 Plan 模板已有 fixture、single-failure、oracle、completion gate，但五项契约仍分散，未形成每个 REQ 的统一机器可读边界卡。
  - [E2][VERIFIED] 现有审计 scope-lock 仅保存 behavior/oracle，未绑定需求边界契约版本或哈希。

  ### RESULT

  推荐先确定唯一事实源：对新的 PLAN_SET 使用 requirements-contract.yaml，由 Plan 索引引用并在 scope-lock 中绑定路径与 SHA-256。旧单文件 Plan 不追溯迁移。这样避免 Blueprint、Plan、测试、审计各自复制一份边界而漂移。

   优先级    目标                                     应更新内容
  ━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   P0        deterministic-implementation-planning    将五项内容提升为每个行为型 REQ 的强制边界契约：固定 YAML 字段/值域、唯一决策表、状态机或不变量、正向与单变量负向 fixture、独立可运行 oracle。缺少成功/失败/非影响边界时必须 BLOCKED-BY-DECISION，不得
                                                      猜测。同步更新 PLAN-TEMPLATE.md、PLAN-SET-TEMPLATE.md、QUALITY-GATES.md、validate-plan.ts 及测试。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P0        requirements-to-test-specification       测试规格必须逐项映射 REQ → 决策表行 → fixture → oracle → 测试 ID；每个失败边界和禁止副作用都需显式断言，状态转换/不变量也必须有对应测试。同步更新 TEST-SPEC-TEMPLATE.md。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P1        blueprint-creation                       增加高层“需求边界账本”：目标成功边界、失败边界、兼容性/非影响边界、开放决策。Blueprint 保留方案比较与现有系统一致性，不提前规定实现符号或错误码；明确交由 Plan 收敛为五项契约。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P1        plan-audit-archiver                      审计从“文件和回执一致”扩展为“实施边界等价”：审计矩阵须覆盖成功、失败、非影响/禁止行为及其证据。scope-lock 只绑定契约引用与哈希，不复制完整 YAML；相应更新 audit 模板和 validate-audit.ts。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P1        test-specification-execution             执行报告增加决策表行、fixture、精确预期结果/错误码、禁止副作用断言、oracle 观测；负向测试必须证明“声明的失败被正确识别”，不只是命令非零。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P2        guided-code-editing                      编辑前先展示 REQ 的成功、失败、非影响边界及 oracle；验收时对照 oracle，不以“测试绿了”结束。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P2        pre-flight-enforcement                   仅加入“契约路径/哈希、所需 oracle”检查，不复制五项 schema。它负责强制使用 Plan 契约，不负责定义契约。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P1        AGENTS.md                                增加简短全局原则：Blueprint→Plan→实施/审计必须显式覆盖成功、失败、非影响边界；Plan 缺边界即阻断；绿色命令不能单独证明需求达成。不要把 YAML 模板塞入全局指南。
  ────────  ───────────────────────────────────────  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
   P2        RULES.md                                 保持其“输出规则”定位，只补一条结论规则：Plan/实施/审计报告 PASS 或 ACCEPT 时，必须有声明成功边界及适用失败/非影响边界的证据，单一绿测不足。可增加 [BOUNDARY] 引用行。

  不建议更新 task-dispatch-router、serve-api、isolated-serve-test 等无关 skill。

  validate-plan.ts、validate-audit.ts 和对应测试的修改属于代码级硬门禁变更，应另行冻结实施范围，不能只改说明文档。当前 validator 文件已有未提交改动，后续实施需先确认其归属并避免覆盖。

  ### CHECK

  - [TEST] N/A（本轮为只读差距整理）
  - [DOC] NOT-UPDATED
  - [RISK] OPEN：需先确认 canonical YAML 契约的落点、兼容策略与解析/校验实现方案。

  ### FINAL

  需要更新的核心不是所有 skill，而是围绕同一条链路的七个：Blueprint 定义高层边界，Plan 固化机器可读契约，测试设计与执行消费契约，审计验证实施边界与契约等价；AGENTS.md 和 RULES.md 只放跨流程原则与结论约束。

## Findings
- 本轮只读盘点，未修改 `AGENTS.md`、`RULES.md`、skills、plans 或 handoff。
- P0：`deterministic-implementation-planning` 与 `requirements-to-test-specification` 固化五项边界契约和映射。
- P1：`blueprint-creation`、`plan-audit-archiver`、测试执行补齐边界定义、等价审计和证据消费。
- P2：`guided-code-editing`、`pre-flight-enforcement` 只消费契约；`AGENTS.md` 写原则，`RULES.md` 写结论约束。

## Decision
- 推荐以 `requirements-contract.yaml` 为唯一事实源，由 Plan 和 scope-lock 绑定路径与 SHA-256；旧单文件 Plan 不追溯迁移。
- `validate-plan.ts`、`validate-audit.ts` 及测试的硬门禁修改须另行冻结；现有未提交改动不可覆盖。

## Verification
- 本次为只读差距整理，未运行测试；后续实施须按冻结范围验证。
