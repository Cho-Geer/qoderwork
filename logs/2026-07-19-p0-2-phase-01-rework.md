# P0-2 PHASE-01 rework — fail-closed cross-run isolation verifier

- 原因: PHASE-01 原 verifier 与固定契约存在 4 处不符（eventState 调布尔 findSessionEvent；dbAvailable 只查文件可打开不探目标表；events 仅 existsSync；attribution 未首失败即 return），且测试仅 10 个手写 case，缺 88 负向矩阵。
- 改动文件（仅 Allowed 两处，types.ts 无需改）:
  - `scripts/test-serve/verify-p02.ts`: 新增 `tableAvailable`（对 session/session_map 做 readonly 表探测，`*EvidenceAvailable` 缺表即 UNAVAILABLE 并短路）；`eventSessionState` 改为逐行解析 JSONL（malformed→UNAVAILABLE，不调 findSessionEvent）；`eventsReadable` 替代 existsSync；attribution 改为 `req` 首失败即 return，后续 check key 不出现；导出 `RUN_PATH_FIELDS`。
  - `scripts/test-serve/__tests__/verify-p02.test.ts`: 新增 88 case 单失败矩阵（6 reservation + 57 field + 25 attribution），断言 exact failedChecks、attribution 前序 true/后序 absent、19 字段 ledger、57 field case、88 唯一 ID、all-pass 每 check true。
- 关键决策:
  - 容器用 manifest 顶层 `rootDir`（paths.rootDir 只是 19 个被包含路径之一），解决 rootDir/manifestPath 字段 mutation 级联。
  - fixture `makeRun` 补设顶层 `rootDir`；字段 mutation 写回 canonical `rootDir/manifest.json`，避免 `manifestPath` 自引用导致写错/覆盖位置。
  - `P02-V-R-PID-A-DEAD` 同步让注入 port-owner reader 返回该死 PID，避免 `reservationPortAOwned` 连带失败。
- 验证: `bun test oracle.test.ts verify-p02.test.ts` → 125 pass / 0 fail / 4647 expect；`git diff --check` OK；scoped diff 仅 verify-p02.ts + verify-p02.test.ts。
- 文档: 更新 `plans/隔离 serve 测试基建待办/p0-2/00-plan-index.md` 中 PHASE-01=DONE、PHASE-02=READY。
- 不越权: 未动 PHASE-02+ 文件；未声明 runtime-smoke（PHASE-05/06 仍 NOT-RUN，端口由 reviewer 提供）。
