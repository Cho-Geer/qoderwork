# 2026-07-19 P0-2 PHASE-03 Sentinel/Orchestrator 返工 3（关闭 D1–D4）

## Why
审计 `audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit-4.md` 判定 REWORK，D1–D4 未关闭。本轮按审计 §5 退出条件实施。

## What
- `p02-sentinel.ts`：`stopSentinel` catch 仅吞 ESRCH（进程已退出），非 ESRCH 信号错误（EACCES/EPERM）改 fail-closed 传播（D2）。
- `p02-orchestrator.test.ts`：`assertNoBusinessAfterFailure` 改用最后一个 `failure` 事件为失败边界（原误用「最后业务事件」导致 `failure→illegal cleanup-b→stop-a` 漏检，D1）；`makeFailAtStageDeps` 每个失败分支 push `failure` 标记；新增 `P02-O-D1-FAILURE-BOUNDARY`（反例必被检出）、`-OK`、`P02-O-D2-SIGTERM-NONESRCH`。
- `03-phase-sentinel-orchestrator.md`：勾选 D1/D2 完成框；新增 1 个开放框（PHASE-03 pending re-audit）以保留 validator 所需 `- [ ]`。
- `00-plan-index.md`：同步为 186 pass、D1/D2 关闭、validator exit 0、PHASE-03 仍 REWORK 待复审。

## Decision
- PHASE-03 不自行标 DONE（审计-4 §5 禁止）；留 REWORK 待复审签核。
- D3 `validate-plan.ts` PLAN_SET 当前工作树已 exit 0（rework-2 已更新索引），本轮仅确认未回退。

## Evidence
- `bun test verify-p02.test.ts p02-orchestrator.test.ts` → 186 pass / 0 fail / 4870 expect（exit 0）。
- 新增 3 用例均 pass：D1-FAILURE-BOUNDARY、D1-FAILURE-BOUNDARY-OK、D2-SIGTERM-NONESRCH。
- `bun run validate-plan.ts plans/隔离 serve 测试基建待办/p0-2` → exit 0（D3）。
- runtime-smoke / live-LLM-E2E：NOT-RUN（PHASE-03 不要求）。
