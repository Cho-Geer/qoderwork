# Phase 2 Steps 1–4, 6 实施（Native Agent 与 DAG 解耦）

**为什么**: 按 `plans/00` 固定顺序推进路线图，Phase 2 目标是普通任务走 native Task、DAG 只做大型 artifact/resume/QoderWork metadata。Step 5（dispatch_subagent 收窄）已在 G3 完成。

**改了什么**（framework-path，grant `06615655` + plan `14eab80f`，已 consumed/completed）:
- `.opencode/service/dispatch/dispatch-validate.ts` — 文件头加 `LEGACY VALIDATOR — NOT in active execution_order` 块（Step 3 point 1）。该函数是 PLAN-FIRST L0-L4 旧 validator，仅经未接线的 `before/dispatch.ts` 可达。
- `.opencode/service/dispatch/index.ts:60` — `validateDispatchBefore` re-export 加 legacy 注释（Step 3 point 4，澄清非 active 路径）。
- `.opencode/service/dispatch/router.ts` — 删除未注册的 `dispatch-sa-repair` no-op caller 分支（`shouldBlock("dispatch-sa-repair")` 恒 false，因 rule 不在 RULE_TABLE）；保留 repair-pattern `matched` 真实校验；删除因此变成死代码的 `getRuleDisposition, shouldBlock` import；policy 标签改为 `super-admin-repair:audit`（Step 3 point 5）。

**验证（static/code）**:
- `bun build` 三个文件均 OK。
- `rg dispatch-sa-repair` 在 `.opencode` 内已无引用。
- legacy 头确认存在。

**已满足（无需编辑）**:
- Step 1: active agent=5（Orchestrator/build/general/plan/explore），仅 Orchestrator.md 自定义 prompt，9 legacy profile；`alias_of` runtime 不消费（final-validation-report 自证）。
- Step 4: `project.config.json` `.opencode/project.config.json` 的 `dispatch_policy.require_dag_entry=false` / `auto_plan_enabled=false` 已设；`plugin_execution_order.before` 不含 `dispatch`。
- Step 6: 无 `scout` agent；`investigation-evidence` + `context7-first` skill 均存在。

**决策 / 偏差**:
- `before/dispatch.ts` 已有 legacy 头（早期加过），Step 3 point 2 免改。
- Step 3 point 3（active docs 删 L0-L4 叙述）：复查发现 active docs 已用新 `getRuleDisposition` 模型，L0-L4 叙述仅存于现已标 legacy 的 `dispatch-validate.ts`，无 active doc 残留 → 免改。
- **runtime 尾巴（需 serve session，非静默遗漏）**：Step 2 的 native Task 无 DAG smoke、Step 5 的 session_events lineage/native metadata grant binding、Step 6 的 explore 复杂调研 smoke，均需在 OpenCode serve 中跑一次 live LLM E2E 才能闭环。

**证据等级**: static/code + structural。runtime smoke 项列为独立 follow-up（并入 Phase 4 runtime smoke 最划算）。
