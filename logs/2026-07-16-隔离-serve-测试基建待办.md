# 隔离 serve 测试基建待办（2026-07-17 复审）

**[ID]**: ISO-SERVE-TODO-20260716-01  
**状态**: `[VERIFIED][INTEGRATION]` P0-1A cleanup 集成已实现且历史执行 PASS；`[BLOCKED]` P0-1B 仅有不合规 runtime supporting evidence；`[NOT-RUN]` live LLM E2E。  
**为什么**: 根据 isolated-serve Blueprint、bootstrap 实施步骤和权限模板测试式样书，校正待办顺序与已完成边界。

**已完成**:
- `bootstrap`：root/child、生产 `createGrant → bindGrant`、isolated SQLite bound oracle、路径/child/import/bind 失败 fail-closed、CLI 非零退出。
- 生命周期组件：PID/端口 reservation、create-release 失败保留 `BLOCKED`/PID/worktree；cleanup 保留 evidence root、manifest、双 DB、logs/events/artifacts/report。
- TSI-05 静态迁移：G2/G3/G4 runner 与 `serve-api-client.ts` 已改为 manifest-only，固定 `4097`、固定 SSE 文件和主 work-one 路径零命中；run-mode 真跑仍待验收。
- 组件验证：`scripts/test-serve/__tests__` 37/37 PASS；Bun parse 与 `git diff --check` PASS。
- P0-1A：真实临时 Git repo + detached worktree 的 cleanup 集成测试已落地；2026-07-17 当次记录为 1/1、规定回归 14/14 PASS。
- P0-1B supporting evidence：第二个 run 有 root/child、bound grant、isolated 双 DB、plan-only artifact 与 cleanup report；因手工写 `READY` 且缺 SSE event，不构成合规 runtime smoke PASS。

**待办（固定顺序）**:
1. `[P0-1A][DONE]` cleanup 确定性集成已实现并有历史 PASS；当前受管沙箱禁止 local bind，本轮复跑被环境阻断，不改写历史裁决。
2. `[P0-1B][BLOCKED]` 用全新 run 严格执行 `create → start → bootstrap → execute(plan) → stop → cleanup`；禁止手工写 manifest，必须由 `start` 自行返回 `READY`，并保存可读的 manifest、双 DB、serve/SSE log、`events.jsonl`、plan artifact 与 cleanup report。
3. `[P0-2][NOT-RUN]` 完成双 run 隔离、SSE 归属、真实 serve 接管 reservation、外部 PID 不受影响的确定性集成证据。
4. `[P0-3][BLOCKED]` 权限模板返工：先跑 `T-PT-048–050`，再补 `T-PT-004` A/B、`T-PT-002`、`T-PT-039–045`、`ADV-PT-008–010`；必须有 fault、20 轮 concurrency、mutation、executor counter 和 target-state oracle。
5. `[P0-4][BLOCKED]` 清 Bun cache、重启隔离 serve，执行 `T-PT-046/051/052`，最后重跑 `T-PT-047`；只有 root/child 正向与全部负向零执行证据齐全，才解除 `BLOCK-PT-02/03`。
6. `[P0-5][BLOCKED]` 仅 reviewer 显式提供 `H2_AUTHORIZED=true` 且 `DRY_RUN=false` 后执行 live LLM E2E；每 case 独立 run ID、identity、DB/SSE/audit/executor/目标状态证据。
7. `[P0-6][NOT-RUN]` 单独完成 TSI-05 runner run-mode 的 `GET /session`、root/child 与 isolated DB 真跑验收；不得用 `execute(plan)` 替代。
8. `[P1][PENDING]` 完成 TSI-06/07 专用 skill 的完整 run evidence 与 runtime 日志归档；随后满足 §2.8 门槛，才删除 `_b_pt_wm_00r2_live.ts` shim。
9. `[P2][NOT-RUN]` 单独执行完整 TypeScript 类型检查，并区分 test-serve 问题与既有 work-one 类型债务。

**决策**: 组件 PASS 不升级为 integration/runtime/live PASS；任何失败保留 `BLOCKED`/`NOT-RUN`。TSI-05 的“迁移”已完成，剩余仅为 run-mode 真跑验收；旧 launcher 在 runtime smoke、所需 live E2E 和调用者扫描通过前不得删除。
