# 2026-07-19 P0-2 PHASE-03 Sentinel/Orchestrator 返工 2

## Why
审计 `audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit-2.md` 判定 Rework：D1 stop-once 在 stop 自身 throw 时失败（stopA/B/sentinel=2）、D2 用聚合 `<=` ceiling 非 ledger、D5 ESRCH 与其他错误混淆且注释漂移、D3/D4 文档状态冲突。

## What
- `p02-orchestrator.ts`：`stopped.a/b/sentinel` 前移到 stop 调用之前（stop-once 在 throw 时仍生效）；`safeConvergeFailure` 不再静默吞错，捕获到 `convergenceErrors` 并随失败结果返回。
- `types.ts`：`P02Result` 失败分支新增 `convergenceErrors?` 字段。
- `p02-sentinel.ts`：`validateSentinelIdentity` 区分 ESRCH(NOT_FOUND) 与其他 probe 错误(UNAVAILABLE)；新增可选 `pidProbe` 注入参数；修正文件头与 JSDoc 中 stale 的 true/false 三态注释。
- `__tests__/p02-orchestrator.test.ts`：新增 3 个 stop 自身 throw 用例（逐对象 `=== 1`）、1 个 convergenceErrors 捕获用例、2 个 ESRCH/EPERM 注入用例；用精确调用事件 ledger(`eventLog` + `assertNoBusinessAfterFailure`) 替换 176 条聚合 `<=` ceiling 断言；stop-throw override 计数前移到 throw 之前。
- `plans/.../03-phase-sentinel-orchestrator.md`：5 项 completion gate 全部勾选。
- `plans/.../00-plan-index.md`：evidence ceiling 更新为 component 达成(183 pass)、baseline 更新为 coordinator 文件已存在、PHASE-03 状态 DONE→REWORK（gate 已过，待复审标 DONE）；PHASE-04 保持 BLOCKED。

## Decision
- D2 ledger 用 kind+object 的有序事件序列，stage 维度由调用顺序隐含表达；断言 firstFailure 后只允许收敛 stop 且每对象 ≤1，比 `<=` ceiling 更严格（能发现补位调用）。
- PHASE-03 不自行标 DONE，留 REWORK 待复审裁决（审计 §7 item 6 要求）。

## Docs updated
- 新增本日志。
- `plans/隔离 serve 测试基建待办/p0-2/03-phase-sentinel-orchestrator.md`：completion gate 勾选。
- `plans/隔离 serve 测试基建待办/p0-2/00-plan-index.md`：evidence ceiling / baseline / PHASE-03 status 同步。

## Evidence
- `bun test verify-p02.test.ts p02-orchestrator.test.ts` → 183 pass / 0 fail / 4867 expect（exit 0）。
- `bun run typecheck` → exit 1，32 条错误全为范围外既有债务（`scripts/_b_l3_012_repo_op_deny.ts`），PHASE-03 四路径 0 诊断。
- tracked `types.ts`：`git diff --check` exit 0；untracked 3 文件 no-index 检查无 whitespace 诊断。
- runtime-smoke / live-LLM-E2E：NOT-RUN（PHASE-03 不要求）。
