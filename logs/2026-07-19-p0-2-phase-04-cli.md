# 2026-07-19 P0-2 PHASE-04 CLI 路由实施

## Why
PHASE-03 已 DONE（audit-5 Accept），按 `04-phase-cli.md` 计划为 `test-serve` 增加单命令 `p0-2` CLI 路由，仅解析固定 flag 并映射 `runP02` 结果（component 级，不触 runtime）。

## What
- `scripts/test-serve/isolated-serve.ts`：command union 增加 `p0-2`；新增 `parseP02Args`（requiredArgs/absoluteInputs/portsDistinct 校验，未知 flag 拒绝）、`getP02Runner`（globalThis 注入），`runTestServeCli(argv)` 导出供测试。success 映射 `ok/status/runA/runB/checks/evidencePaths.stageResults`，failure 映射 `ok/firstFailure/evidencePaths.stageResults`，均 exit 1。
- `scripts/test-serve/__tests__/p02-cli.test.ts`：6 个 P02-C 用例（MISSING/PORT/PATH/FAIL/合法/未知flag）覆盖 Check Registry 全项；`p02-cli-harness.ts` 捕获 stdout/stderr/exitCode。
- `00-plan-index.md`、`04-phase-cli.md`：PHASE-04 升 DONE，completion gate 维持 `[ ]` 合同形态。

## Decision
- `runP02` 通过 `globalThis.__P02_RUNNER__` 注入 spy，CLI 不启动真实 coordinator（component 级）。
- 拒绝相对/不存在 path、空 testId、端口非 1–65535、端口相同、缺 flag、未知 flag；全部校验在调用前完成，非法输入 coordinator 调用数为 0（REQ-001）。

## Evidence
- `bun test scripts/test-serve/__tests__/p02-cli.test.ts scripts/test-serve/__tests__/p01b-orchestrator.test.ts` → 43 pass / 0 fail。
- `bun run scripts/test-serve/isolated-serve.ts --help` → 末行含 `test-serve p0-2 ...`。
- `git diff --check -- isolated-serve.ts p02-cli.test.ts p02-cli-harness.ts` → 无 whitespace 错误。

## Updated docs
- `scripts/test-serve/isolated-serve.ts`（modify）
- `scripts/test-serve/__tests__/p02-cli.test.ts`（add）
- `scripts/test-serve/__tests__/p02-cli-harness.ts`（add）
- `plans/隔离 serve 测试基建待办/p0-2/00-plan-index.md`（PHASE-04 → DONE）
- `plans/隔离 serve 测试基建待办/p0-2/04-phase-cli.md`（completion gate 注释）
- `logs/2026-07-19-p0-2-phase-04-cli.md`（本日志）
