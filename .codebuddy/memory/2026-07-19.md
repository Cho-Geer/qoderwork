# 2026-07-19

## P0-2 隔离 serve 测试基建（plans/隔离 serve 测试基建待办/p0-2）
- PHASE-02（生命周期/cleanup verifier current observation）已完成：145 pass / 0 fail；改动 verify-p02.ts / types.ts / verify-p02.test.ts 三个 Allowed 文件。
- PHASE-03（sentinel/orchestrator）已 READY；禁止文件 process.ts/run-context.ts/bootstrap.ts/oracle.ts 未动。
- 约定：根 `bun run typecheck` 持续 exit 1 为范围外既有错误（含 RunManifest.rootDir 类型缺口、PHASE-01 reservation case），按计划 DEC-004 不阻断 PHASE-02/03，仅 runtime/closure 阶段（PHASE-07/08）才要求根 typecheck 通过。
- 验证证据层级：当前为 component，runtime 仍为 NOT-RUN（需 reviewer 提供 P0_2_PORT_A/B）。
- 审计补齐：audits/p0-2/2026-07-19-phase-02-lifecycle-audit.md 唯一 non-blocking 发现 D1（closure 日志 after-stop-a 字段列表漏列 bSseReadyValid）已修复；代码本身正确。

## CodeBuddy skill 扫描机制（来源：.codebuddy-server-cn/.../kimi-code-0.6.3/extension.js agent-core scanner）
- `resolveSkillRoots` 聚合顺序（约 79032）：① explicitDirs（来自 this.skillDirs，非空则**独占**覆盖下方默认，79040-79046）→ ② 项目 brand `<root>/.kimi-code/skills`（CodeBuddy 重品牌为 `.codebuddy/skills`）→ ③ 项目 generic `<root>/.agents/skills` → ④ 用户 brand `<home>/.kimi-code/skills` → ⑤ 用户 generic `<home>/.agents/skills` → ⑥ extraDirs（配置 extraSkillDirs）→ ⑦ pluginSkillRoots（已启用 plugin 的 manifest.skills）→ ⑧ builtinDir。按 skill name 去重。
- 磁盘实测：qoderwork 的 `.codebuddy/skills` 与 `.agents/skills` 含**完全相同 30 个 skill**，均进入 available_skills（location: project）。
- 运行时权威列表：`session.listSkills()`（160459）与 `listWorkspaceSkills({workDir})`（160469）调用同一 resolveSkillRoots；agent 侧 available_skills 即该输出。
- 删除 `.codebuddy/skills` 是否回退 `.agents/skills`：取决于该目录是 brand 还是 explicitDir 身份。this.skillDirs 为空→brand 身份→`.agents/skills` 仍被扫（镜像，集合不变）；非空（设为该目录）→explicitDir 身份→无回退。结论仍为 UNVERIFIED，需查设置 skillDirs/extraSkillDirs 才能定论；AGENTS.md 禁止删除 `.codebuddy/`。

## ~/.codebuddy 与 ~/.codebuddy-server-cn 角色区分（实测目录结构）
- `~/.codebuddy/` = CodeBuddy 的 **home/配置目录**（kimi-code 源中等价 `~/.kimi-code`）：含 `skills/`、`plugins/`、`skills-marketplace/`、`rules/`、`mcp.json`、`settings.json`。skill 配置落点在此。
- `~/.codebuddy-server-cn/` = **IDE 服务端运行时/扩展安装目录**：含 `bin/`、`data/`、`extensions/`（kimi-code 扩展 bundle 在此）。不可在此配 skill。
- 配置文件名（来自 kimi-code 源码 55058-55062 + 重品牌推断）：全局 `<homeDir>/config.toml` → `~/.codebuddy/config.toml`；项目级 `<root>/.kimi-code/local.toml` → CodeBuddy 重品牌为 `<root>/.codebuddy/local.toml`。`extraSkillDirs` 写这两处；`skillDirs`(explicitDirs) 是宿主启动选项，配置文件无法设。
- `~/.codebuddy/settings.json` 仅含 `enabledPlugins`（插件开关），不含 skill 目录。

## 重启后当前 skill 扫描/加载路径（实测 2026-07-19）
- 用户已把 `~/.codebuddy` 项目级 `local.toml` 从 `<root>/.agents/skills`（无效占位符）改为 `extraSkillDirs = [".agents/skills"]`（相对 projectRoot，有效）。但该目录本就是默认 project-generic 根 → **冗余**，加载集合不变。
- 当前生效扫描/加载路径（重启后实测）：
  1. 项目 brand `/home/zhaoge/workspace/qoderwork/.codebuddy/skills/` = 26 个 skill（主集合，location:project）
  2. 项目 generic `/home/zhaoge/workspace/qoderwork/.agents/skills/` = 同样 26（镜像，去重无新增）
  3. 用户 brand home `/home/zhaoge/.codebuddy/skills/` = 19 个（项目 26 的子集，无新名）
  4. 用户 generic home `/home/zhaoge/.agents/skills/` = 不存在（跳过）
  5. manager `/home/zhaoge/.codebuddy/skills-marketplace/skills/`（create-skill/find-skills/skill-creator/多模态内容生成）
  6. plugin `/home/zhaoge/.codebuddy/plugins/`（agent-browser/playwright-cli）
  7. builtin 打包在 extension.js
- `available_skills` 的 26 个 location:project 与项目磁盘 26 目录一一对应（VERIFIED 交叉核对）。
- 结论：`extraSkillDirs` 是追加语义，无法实现「仅 .agents/skills」；那需要 skillDirs(explicitDirs) 启动参数（配置文件设不了）。

## P0-2 PHASE-03 Sentinel/Orchestrator 返工（2026-07-19，pre-flight-enforcement 指导）
- 审计 `audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit.md` 判定 Rework（D1–D8）。已实现最小返工包并复跑固定命令：177 pass / 0 fail / 4799 expect（exit 0），`git diff --check` 4 文件 = 0。
- 关键修订：① `SentinelIdentityState` 三态（FOUND/NOT_FOUND/UNAVAILABLE），调用点改 `=== "FOUND"`；② `validateInput` 对 primaryWorktree/mainFrameworkDbPath 用 `node:path.isAbsolute` fail-closed；③ stop-once 在 `stop-a/stop-b/stop-sentinel` 正常成功后即置位 `stopped`（修复晚期失败重复 stop，aStopCount=2→1）；④ `P02Result` 失败分支新增 `stages` ledger，16-stage 失败矩阵断言前缀。
- 范围：仅 4 个 Allowed 文件（p02-sentinel.ts/p02-orchestrator.ts/types.ts/__tests__/p02-orchestrator.test.ts）；forbidden 文件未动。
- `bun run typecheck` 仍 exit 1，但全为 work-one `.opencode/service/*` 与 `scripts/_b_l3_012_repo_op_deny.ts` 既有债务，本 rework 0 新增错误。
- 状态：PHASE-03 实现完成待复审（plan index 改 REWORK），PHASE-04 仍 BLOCKED。runtime-smoke/live-E2E NOT-RUN（PHASE-03 不要求）。
- D8：`RunManifest.rootDir?` 为既有通用字段（非本 rework 新增），仅记录归属，未扩大 anchor。
- 【更正 2026-07-19 闭环】上条 D8 声明为假：`git blame -L 124,124 types.ts` 证实 `RunManifest.rootDir?` 为未提交新增（Not Committed Yet 2026-07-19），非既有字段。已扩展 PHASE-03 plan 的 types.ts anchor 显式纳入该字段（被 verify-p02.ts:317 `mfa.rootDir` 读取，属 P02 合同表面），并更正 rework 日志。
- D4 闭环：`P02-O-STAGE` 循环新增 `callCeilingAfter` 独立 dependency-call counter ledger（6 业务 + 5 verify 计数 ≤ stage 上限，收敛 stop/stopSentinel 由 P02-O-STOP 单独记账），expect 4799→4975（+176=16×11）。
- PHASE-03 闘包：5 项 completion gate 全过（177 pass/0 fail/4975 expect、git diff --check 4 文件=0、typecheck exit 1 但 PHASE-03 路径 0 诊断，32 错误全为 work-one/.opencode/service/* 与 _b_l3_012_repo_op_deny.ts 既有债务）；plan index PHASE-03 REWORK→DONE，PHASE-04 仍 BLOCKED。证据层级仍为 component（runtime NOT-RUN，PHASE-03 不要求）。

## P0-2 PHASE-03 返工 2（audit-2 Rework 裁决后）
- 审计 `audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit-2.md` 判 Rework：D1 stop-once 在 stop 自身 throw 时失败（stopA/B/sentinel=2）、D2 聚合 `<=` ceiling 非 ledger、D5 ESRCH 与其他错误混淆 + 注释漂移、D3/D4 文档状态冲突。
- 返工完成：①stop-attempt 标志前移到 stop 调用前 + safeConvergeFailure 捕获 convergenceErrors（不再静默吞错）+ P02Result 增 convergenceErrors 字段；②3 个 stop-self-throw 用例逐对象 `=== 1` + 1 个 convergenceErrors 捕获用例；③精确调用事件 ledger(eventLog + assertNoBusinessAfterFailure) 替换 176 条聚合 `<=` ceiling；④validateSentinelIdentity 区分 ESRCH(NOT_FOUND)/其他(UNAVAILABLE) + 可注入 pidProbe + 修正 stale true/false 注释；⑤2 个 ESRCH/EPERM 注入用例。
- 验证：183 pass/0 fail/4867 expect；typecheck exit 1 范围外 32 错误，PHASE-03 路径 0 诊断；tracked/untracked whitespace 均无诊断。
- 状态：PHASE-03 plan index DONE→REWORK（gate 已过，待复审标 DONE），PHASE-04 仍 BLOCKED。runtime NOT-RUN。logs/2026-07-19-p0-2-phase-03-rework-2.md 已记。

## P0-2 PHASE-03 返工 3（audit-4 D1–D4 关闭，2026-07-19）
- 审计 `audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit-4.md` 判 Rework（D1–D4）。本轮按 §5 退出条件实施。
- D2：`p02-sentinel.ts` 的 `stopSentinel` catch 仅吞 ESRCH，非 ESRCH 信号错误（EACCES/EPERM）改 fail-closed 传播。
- D1：`assertNoBusinessAfterFailure` 改用最后一个 `failure` 事件为失败边界（原「最后业务事件」导致 `failure→illegal cleanup-b→stop-a` 漏检）；`makeFailAtStageDeps` 每失败分支 push `failure` 标记；新增 `P02-O-D1-FAILURE-BOUNDARY`(反例必被检出)/`-OK`/`P02-O-D2-SIGTERM-NONESRCH`。
- D3：`validate-plan.ts` PLAN_SET 在当前工作树已 exit 0（rework-2 已更新索引），本轮仅复验未回退。
- D4：`00-plan-index.md` 同步 186 pass / D1/D2 关闭 / validator exit 0 / PHASE-03 仍 REWORK 待复审；`03-phase-sentinel-orchestrator.md` 勾选 D1/D2 框并保留 1 个开放框（pending re-audit）以满足 validator 所需 `- [ ]`。
- 验证：186 pass / 0 fail / 4870 expect（exit 0）；3 新用例均 pass；validator exit 0。PHASE-03 不自行标 DONE（audit-4 §5），留 REWORK 待复审签核。runtime NOT-RUN。
- 日志：logs/2026-07-19-p0-2-phase-03-rework-3.md。

## P0-2 PHASE-03 audit-6 复审（re-audit of audit-5，2026-07-19）
- 复审 `audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit-5.md` 的 Accept 判定，采用 pre-flight-enforcement + plan-audit-archiver 组合。
- D1–D4 逐项运行态复核 + 补做 negative control mutation 验证（Step 4 要求）：D1/D2 的 buggy 实现下测试会 FAIL、正确实现下 PASS，敏感性已证明。
- 测试基线 186 pass/0 fail/4870 expect 与 audit-5 一致；PHASE-03 文件 untracked，无 PHASE-04 污染。
- 判定：Accept（component 级维持 audit-5）。
- 关键发现：①audit-5 §1 未声明 provenance 缺口（scope-lock/evidence/validate-audit.ts 机器门均缺失，与 audit-4 §10 一致），v2.1 正式 ACCEPT 仍 BLOCKED；②plan 文档 completion gate box 全部 `[ ]` 未勾选（与 rework-3 memory「勾选 D1/D2 框」声明矛盾，实际未勾选），NON_BLOCKING_DEBT F-001；③PHASE-04 已 DONE（00-plan-index:113，audit-5 之后演进，未复审）。
- 产物：audits/p0-2/2026-07-19-phase-03-sentinel-orchestrator-audit-6.md（146 行）+ LATEST.md 更新 + logs/2026-07-19-p0-2-phase-03-audit-6.md。

## P0-2 PHASE-04 CLI 独立审计（generation 1，2026-07-19）
- PHASE-04 已实施（logs/2026-07-19-p0-2-phase-04-cli.md，43 pass）但从未审计，本轮首次独立审计。
- Fixed verification 3 命令全 pass：bun test p02-cli+p01b → 43 pass/0 fail；--help 含 p0-2；git diff --check exit 0。
- REQ-001/002/003 满足；Check Registry 5 项满足；negative control mutation 验证 PASS（3 用例敏感性已证明）。
- forbidden files 未违反：p02-orchestrator.ts 未改、无 --retry/--skip/H2/DRY_RUN 禁止 flags。
- 判定：Accept（component 级）。标注 3 项 NON_BLOCKING_DEBT：F-001 failure JSON 缺 controlledStops（P02Result 无此字段，plan Fixed contract line 43 与类型不一致，需 plan 作者澄清 controlledStops 是否 === convergenceErrors）；F-002 absoluteInputs 不检查 existsSync（component 级简化）；F-003 p02-cli-harness.ts 不在 Allowed files（测试辅助 scope 越界）。
- v2.1 正式 ACCEPT 仍 BLOCKED（provenance 缺口，与 PHASE-03 一致）。
- 产物：audits/p0-2/2026-07-19-phase-04-cli-audit.md（157 行）+ LATEST.md 更新 + logs/2026-07-19-p0-2-phase-04-audit.md。
- 文本产物闸门触发：日志首次写入文件为 0 字节，fail-closed 恢复后验证通过。

## P0-2 PHASE-03/04 debt 记录 + audit-6 F-001 修正（2026-07-19）
- 任务 1：将 PHASE-03(audit-6) 与 PHASE-04(phase-04-cli-audit) 的 NON_BLOCKING_DEBT 记录到 `debt/隔离 serve 测试基建待办/p0-2/2026-07-19-phase-03-04-non-blocking-debt.md`（85 行）。
- 共 6 项发现：F-03-001（completion gate 全 [ ]，项目约定非缺陷）、F-03-002（audit-5 未声明 provenance 缺口）、F-04-001（failure JSON 缺 controlledStops，需 plan 澄清）、F-04-002（absoluteInputs 不检查 existsSync，component 级简化）、F-04-003（p02-cli-harness.ts scope 越界）、F-COMMON-001（跨 phase v2.1 provenance 缺口）。
- 任务 2：修正 audit-6 F-001 分类（3 处引用：L92 主体、L133 §1、L140 §10）从"NON_BLOCKING_DEBT（文档一致性）"改为"项目约定（非缺陷）"，依据 04-phase-cli.md:106 声明。rework-3 log"勾选 D1/D2 框"措辞澄清为"在 box 文字添加 closed 引用"。
- Todo #2 完成。

## Provenance 流程约定落地（AGENTS.md §15 + skill Invariant 14-16，2026-07-19）
- 根因分析：provenance 缺口根因是实施流程（先实施后审计）与审计规范（先冻结再实施）的时序冲突 + 工具链路径依赖 + plan-skill 契约缺失 + 判定标准不一致。
- V2 方案落地（3 指摘回应：通用性 + 收拢 + 精准）：
  - `AGENTS.md` 新增 §15（L466-531）：通用规则 P-01~P-06，四要素结构（约束主体+触发条件+违反判定+违反后果），RFC 2119 强约束措辞。适用于所有 plan，不限于 p0-2。
  - `plan-audit-archiver/SKILL.md` 新增 Invariant 14-16（L65-87）：Pre-change receipt 前置验证 + capture-state.ts 唯一合法性 + validate-audit.ts 机器门必要性。
  - `00-plan-index.md`（p0-2）头部新增 Provenance level 声明（L9-11）：PHASE-01~04 component-only（历史），PHASE-05~08 v2.1-required。
- 不另建 SOP 文档；规则全部收拢到 AGENTS.md（通用）+ skill（执行约束）。
- 文件变更：AGENTS.md 469→536 行，SKILL.md 343→373 行，00-plan-index.md 118→121 行。全部通过文本产物写入完整性闸门。

## plan-audit-archiver skill scripts/templates 同步更新（2026-07-19）
- 审核 SKILL.md 变更后 scripts/templates 是否需要同步：4 文件需更新，2 文件不需要。
- 落地修改（4 文件同步，字段名一致，测试 42 pass / 0 fail）：
  - `SKILL.md` Invariant 15 修正（L73）：capture-state.ts 生成仓库状态 receipt，execution receipt 由 trusted runner 生成并绑定到 verdict-state receipt（via repository_state_sha256）。
  - `scope-lock-template.json`（L13）：scope 节增加 `provenance_level` 字段。
  - `audit-report-template.md`（L172-173 + L302-345）：JSON contract 增加 `inherited_blockers[]` + `downgrade_declaration`；增加说明文档。
  - `validate-audit.ts`（1234 行）：新增常量 + checkInheritedBlockers/checkDowngradeDeclaration 函数；P-01/P-04/P-05/P-06 验证逻辑。
  - `validate-audit.test.ts`（1097 行）：Contract type + baseContract 增加新字段；evidence_ceiling 从 component 改为 integration。
- 关键修正：P-06 从 `evidenceCeiling === "component"` 改为 `provenanceLevel === "component-only"`（P-06 约束 plan 级别，不是证据级别）。
- 测试：42 pass / 0 fail。
