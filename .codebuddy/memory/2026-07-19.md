# 2026-07-19

## P0-2 双 run 确定性隔离（plan: plans/隔离 serve 测试基建待办/p0-2/）
- 8 个 phase 严格顺序依赖，每 phase 有固定验证命令与完成门。
- **PHASE-01 已完成**（2026-07-19）：`verifyP02` 的 reservations/attribution 改为 fail-closed 三态 `FOUND|NOT_FOUND|UNAVAILABLE`；新增注入式 `portOwnerReader`；reservations 增 `pathContainedA/B.*`；attribution 拆 availability→positive→negative→grantBound 短路。证据：`bun test oracle.test.ts verify-p02.test.ts` 45 pass/0 fail，`git diff --check` 干净，禁项布尔取反已移除。
- **PHASE-02 现 READY**：lifecycle/cleanup verifier（coexistence/after-stop-a/cleanup 仍布尔，待改 current-observation singleton）。
- 阻断项：PHASE-05/06 需 reviewer 提供两组不同端口；PHASE-07/08 被根 typecheck exit 1（范围外债务）阻断。
- 关键约定：oracle.ts public API 不得改；verifier 不得向 DB/manifest/events 写入；低层 component PASS 不得宣称 runtime-smoke/DONE。
