# 2026-07-18

## P0-2 双 run 隔离计划 — Phase 0 基线审计（PASS）

- 执行 `plans/隔离 serve 测试基建待办/p0-2/01-p0-2-双run确定性隔离实施计划.md` 的 Phase 0（冻结基线与证据清单，只读审计，无文件改动）。
- 证据：codegraph status up to date（299 files）；git status clean；主 framework DB readonly 可打开；五符号(createRunContext/startRunProcesses/stopRunProcesses/bootstrapRun/verifyP01b)调用面与计划一致；非 runtime 11 文件 92/92 PASS、423 expect()。
- 未决项：`P0_2_PORT_A`/`P0_2_PORT_B` 由 reviewer 在 Phase 5/6 提供（env 当前 UNSET），属运行时前置，不阻断 Phase 0。
- 下一步：Phase 1 新建 `scripts/test-serve/oracle.ts`（只读 SDK/session_map/grant/event 查询）+ 复用至 verify-p01b + oracle.test.ts。
- 约束：禁止修改 process.ts/run-context.ts/bootstrap.ts；任何新 runtime 缺陷必须标 BLOCKED 保留现场，不机会主义补丁。

## P0-2 Phase 1 设计决策（据 logs/2026-07-18-P0-2实施计划审计.md line 12）

- 审计更新：Phase 1 oracle 仅 5 个只读函数（findSessionEvent/sdkSessionExists/sessionMapExists/grantExists/grantBoundTo），**不扩张**共享 API。
- `querySdkDbParent`（childParentMatchesRoot 用）保持本地于 verify-p01b.ts，不进 oracle。
- 新增第 4 项文件变更：verify-p01b.test.ts 增加 `childParentMatchesRoot` 的 parent_id mismatch 独立负向用例。
- Phase 1 代码修改清单 = 4 文件：oracle.ts(新)、verify-p01b.ts(改)、oracle.test.ts(新)、verify-p01b.test.ts(改)。
- 固定验证：`bun test oracle.test.ts verify-p01b.test.ts` 全绿；保留 verify-p01b 现有 5 例 + 新负向例不回归。

## P0-2 Phase 1 完成（VERIFIED，2026-07-18）

- 用户已落盘 4 文件变更并验证：`bun test oracle.test.ts verify-p01b.test.ts` → **13 pass / 0 fail / 43 expect()**（oracle 7 例 + verify-p01b 6 例，含新 childParentMatchesRoot parent_id mismatch 负向例）。
- oracle.ts 5 只读函数建立，Phase 2 的 verify-p02.ts 可直接复用（grantExists/sessionMapExists 等）。
- 现状：Phase 0 基线 PASS + Phase 1 VERIFIED。下一步 Phase 2：verify-p02.ts 跨 run 五组 checks（reservations/coexistence/attribution/after-stop-a/cleanup）；仍待 reviewer 提供 P0_2_PORT_A/B 才到 Phase 5/6 runtime。

## P0-2 Phase 2 设计指引（已交付，未落盘，2026-07-18）

- 计划 §6 Phase 2：新建 verify-p02.ts（verifyP02(input, phase)，5 阶段）+ verify-p02.test.ts（单点失败矩阵）。不改 verify-p01b.ts / oracle.ts。
- 3 个待确认设计假设（影响 Phase 3 编排器对接）：① 原 reserver PID 由编排器经 input 传入（manifest release 后 portReserverPid=null）；② 四项 start checks 由编排器写入 artifactsDir/p0-2-stage-results.json 的 start-a/start-b 阶段，verifier 读取判定；③ sentinel 状态由 sentinelMarkerPath 的 pid + /proc 存活判定。
- verifyP02 签名为 P02VerifyInput { runDirA, runDirB, mainFrameworkDbPath, sentinelMarkerPath, reservationPidA?, reservationPidB? } + phase。
- 固定验证：bun test oracle.test.ts verify-p01b.test.ts verify-p02.test.ts 全绿。

## P0-2 Phase 2 设计假设复审裁决（全部可行，2026-07-18）

- 假设①（reserver PID 经 input 传入）：**采纳**。process.ts:49-52 证实 start 后 manifest.portReserverPid=null，coexistence 阶段离开 input 无法判定"原 reserver 已退出"；reservations/coexistence 两 phase 缺失时必须 fail-closed。
- 假设②（start checks 嵌入 stage results）：**采纳**。types.ts 证实 manifest 不存 StartChecks；但 after-stop-a 的"B 仍 READY"不能用 start-b 旧快照，Phase 3 编排器须在 stop-a 阶段嵌入 `inspectRunProcesses(B)` 纯读快照，保持 verifier 纯文件驱动。
- 假设③（sentinel marker + /proc）：**采纳**。marker 格式与计划 Phase 3 step 1 逐字一致；存活/退出判定须校验 `/proc/<pid>/environ` 含 `QODERWORK_P0_2_SENTINEL_ID=<sentinelId>`（对齐 validateRunProcess 范式），不得只查 PID 存在（防 PID 复用假阳性）。
- P02VerifyInput 形状属 Phase 2 设计自由度，不在计划 §5.2 P02Input 固定字段约束内。

## P0-2 Phase 1 独立复审（PASS，2026-07-18）

- 复审发现新负向用例与修订后计划 step 6 两处不符：参数 `"wrong-root"`→计划固定 `"unexpected-parent"`；`toContain`→精确 `toEqual(["childParentMatchesRoot"])` 并补 `checks.childParentMatchesRoot === false`。
- 已按 step 6 逐字收紧并复跑：`bun test oracle.test.ts verify-p01b.test.ts` → 13 pass / 0 fail / **44 expect()**；全量非 runtime 回归 12 文件 → 100 pass / 0 fail / 445 expect()；`git diff --check` OK。
- 既有类型债务：`tsc --noEmit` 报错全部位于 work-one `.opencode/service/**` 与 `_b_l3_012_repo_op_deny.ts`，非 Phase 1 引入，已记录不阻断。
- 计划文档已更新：状态 `[PHASE1-PASS][READY-FOR-PHASE2]`，§6 Phase 1 裁决块落盘，§9 完成门第 2 项勾选。

## P0-2 Phase 2 完成（VERIFIED，2026-07-18）

- 用户落盘 verify-p02.ts（verifyP02 五阶段只读 verifier）+ verify-p02.test.ts（单点失败矩阵）。我修复了 @verify-p02.test.ts:122-123 占位后暴露的 3 个失败，并修正 verify-p02.ts 一处 verifier 健壮性缺陷。
- 修复 1（verifier 健壮性）：`verifyP02` 原在顶层裸调 `readRunManifest(input.runDirA/B)`，manifest 缺失时抛 ENOENT 而非返回 failedChecks。新增 `safeReadManifest`（缺失返回 `{readable:false}`），cleanup 阶段改用 `readA.readable`/`readB.readable` 判定 aManifestReadable/bManifestReadable，并在 manifest 缺失时跳过其内容依赖检查（不误判）；非 cleanup 阶段缺失时 fail-closed 直接返回 `{ok:false}`（不改计划规定的 check 名集合）。
- 修复 2（测试数据）：`reservations: all pass` 原用假 PID 999001/999002（系统上可能存活），改用 `process.pid`/`process.ppid`（确为存活且互异），使 reservationPidAAlive/BAlive/Distinct 断言可靠。
- 验证：`bun test oracle.test.ts verify-p01b.test.ts verify-p02.test.ts` → **44 pass / 0 fail / 83 expect()**；`tsc --noEmit` 对 Phase 2 文件零错误（残留错误均位于 work-one 与 `_b_l3_012_repo_op_deny.ts`，非本次引入）。
- 下一步：Phase 3 编排器接入（sentinel marker 写入须校验 `/proc/<pid>/environ` 含 `QODERWORK_P0_2_SENTINEL_ID`）；仍待 reviewer 提供 P0_2_PORT_A/B 才到 Phase 5/6 runtime。

## P0-2 Phase 2 独立交叉审核纠偏（REVIEW-FAIL，2026-07-18）

> 本节覆盖上方“Phase 2 完成（VERIFIED）”结论。`44 pass / 0 fail / 83 expect()` 已复跑确认，但它仅代表当前组件测试通过，不能证明 Phase 2 计划验收完成；Phase 3 当前为 BLOCKED。

- attribution 存在 fail-open：测试的 all-pass 场景没有创建 B SDK/framework/events 和主 framework DB，`!sdkSessionExists`、`!sessionMapExists` 等仍把缺失证据判为负向隔离成功。
- attribution 缺少 A grant 与 root/child 的绑定检查，也缺少 B grant 对 A grant 的负向检查；`grantBoundTo` 已导入但未使用。
- reservations 只比较部分路径，未覆盖全部 RunPaths 字段，也没有 root containment/realpath 防逃逸检查。
- after-stop-a 只复用历史 start-b checks 并检查 SSE marker 存在；未使用当前 B serve/SSE identity 与 health，也未校验 marker 内容。
- sentinel 只检查 `/proc/<pid>` 存在，未校验 marker sentinelId 与 `/proc/<pid>/environ` identity，存在 PID 复用假阳性；cleanup 缺 marker 时还会误判 sentinel 已退出。
- cleanup 未验证双 DB、日志、events 仍可读，也未要求 cleanup report 的 worktreeRemoved === true。
- 非 cleanup manifest 缺失会返回 ok:false 但 failedChecks 为空；不满足可诊断的 fail-closed 结果。
- verify-p02.test.ts 未做到“每个 check 单独失败”：reservations 仅覆盖 port 冲突，attribution 无独立负向用例；现有 after-stop/cleanup all-pass fixture 也没有提供真实当前进程或完整持久证据。
- 固定返工步骤已写入 P0-2 计划 Phase 2 末尾；完成全部证据门、逐 check 矩阵及回归前，不得开始 Phase 3。
- 类型检查现状：全量 `tsc --noEmit` 仍因 work-one `.opencode/service/**` 与 `_b_l3_012_repo_op_deny.ts` 既有债务失败；本次输出未命中 Phase 2 文件，因此只能记录“未发现 Phase 2 新类型错误”，不能写成全量 PASS。
