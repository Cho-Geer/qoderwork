# OpenCode 框架工具调用失败链与返回方式（完整版）

> **版本**: v2.0.0
> **生成日期**: 2026-07-03 | 基于 work-one 项目实际代码
> **目的**: 完整记录工具调用的成功/失败链、返回格式、失败检测逻辑、计数器行为、完整工具清单

---

## 目录

1. [工具调用完整链路](#一工具调用完整链路)
2. [工具返回值结构标准体系](#二工具返回值结构标准体系)
3. [失败检测逻辑](#三失败检测逻辑)
4. [计数器与阈值](#四计数器与阈值)
5. [各工具的实际返回格式（37 个自定义工具）](#五各工具的实际返回格式37-个自定义工具)
6. [MCP 工具返回格式（25+ 个）](#六mcp-工具返回格式25-个)
7. [完整工具清单与返回格式速查表](#七完整工具清单与返回格式速查表)
8. [完整失败类型矩阵](#八完整失败类型矩阵)
9. [附录：关键代码索引](#九附录关键代码索引)

---

## 一、工具调用完整链路

### 1.1 三层阻断链路（完整流程）

```
┌─ Agent 决定调用工具 ──────────────────────────────────────────────┐
│                                                                     │
│  Step 1: before-hook 链执行（14 个 handler，顺序执行）          │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 1. anti-bypass (FIRST) — 检查 guidance gate          │    │
│  │    → 如果 guidance gate 激活 → throw（阻断所有工具）    │    │
│  │    → 如果 cumulative failures >= hardThreshold → throw  │    │
│  │    → 如果 compliance_blocks >= complianceThreshold     │    │
│  │        → throw（框架合规阻断）                        │    │
│  │                                                      │    │
│  │ 2. phase0-enforce — initial_read 阶段工具限制      │    │
│  │ 3. codegraph — impact 分析检查                        │    │
│  │ 4. config-guard — git hook bypass 防护                │    │
│  │ 5. scope — 写入作用域验证                             │    │
│  │ 6. gate — gate armed 检查                           │    │
│  │ 7. checklist — 执行清单阶段门控                         │    │
│  │ 8. dispatch — PLAN-FIRST 路由验证                     │    │
│  │ 9. uc7ks — UC7KS 知识管线强制                       │    │
│  │ 10. tdd — TDD 强制执行                               │    │
│  │ 11. task — DISPATCH-INTEGRITY 哈希校验                 │    │
│  │ 12. git-guard — git hook bypass 阻断                    │    │
│  │ 13. question-policy — 提问工具策略                     │    │
│  │ 14. json-validate — JSON 语法验证                      │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  如果任何 before-hook throw → 工具不执行 → after-hook 不触发    │
│  错误返回 Agent，由 anti-bypass 追踪                           │
│                                                                     │
│  Step 2: 工具 execute() 执行 ────────────────────────────    │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 工具执行（如 safe_edit, safe_shell, read, ...）       │    │
│  │ → 成功：返回 output                                  │    │
│  │ → 失败：返回 output（含错误信息）                      │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  Step 3: after-hook 链执行（14 个 handler，顺序执行）       │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │ 1. scope — 更新 eslint-state.json dirty_modules        │    │
│  │ 2. format — 自动格式化（非阻断）                     │    │
│  │ 3. read-track — 追踪读事件                          │    │
│  │ 4. task — 记录任务完成                             │    │
│  │ 5. audit — 追加写审计追踪                          │    │
│  │ 6. tdd — 写后 TDD 验证                            │    │
│  │ 7. uc7ks — 追踪知识管线合规性                      │    │
│  │ 8. gate — 清理过期 gate sessions                     │    │
│  │ 9. cache — 同步 knowledge_cache_state                 │    │
│  │ 10. dispatch — 清理 dispatch 状态                     │    │
│  │ 11. anti-bypass — 检测失败 + rewardReport()          │    │
│  │ 12. codegraph — 追踪 codegraph_explore 调用          │    │
│  │ 13. db-health — 监控 DB 健康状态                     │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                     │
│  after-hook 的 throw 不会阻断工具（工具已执行完）              │
│  错误仅记录到日志                                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 关键设计原则

| 原则 | 说明 |
|------|------|
| **Before = 阻断** | 任何 before-hook throw → 工具不执行，Agent 收到错误 |
| **After = 副作用** | after-hook 错误不阻断工具，仅记录 |
| **anti-bypass FIRST** | before 链第一个执行，防止其他 plugin 抢占强制执行 |
| **Orphan 检测** | before-hook 执行但 after-hook 未执行 → 框架合规阻断 |

---

## 二、工具返回值结构标准体系

### 2.1 MCP 标准返回格式（MCP Tools）

所有 MCP 工具必须遵循 MCP 标准：

```typescript
// 成功返回
{
  content: [{ type: "text", text: "<JSON 字符串或文本>" }],
  isError: false
}

// 失败返回
{
  content: [{ type: "text", text: "<错误信息 JSON 字符串>" }],
  isError: true
}
```

- `content`: 数组，每个元素是 `TextContent` 对象
- `text`: 通常是 `JSON.stringify(result)` 后的字符串
- `isError`: 框架用来判断工具是否执行成功

**实际代码验证**（`compliance-gate.ts`）：

```typescript
// 成功：status === "armed"
return { content: [{type: "text", text: JSON.stringify(result)}], isError: false };

// 失败：status !== "armed"
return { content: [{type: "text", text: JSON.stringify(result)}], isError: true };
```

### 2.2 Custom Tool API 返回格式

使用 `@opencode-ai/plugin` 的 `tool()` helper 定义的自定义工具：

```typescript
// 成功：返回任意值（框架自动包装）
async execute(args, context) {
  return "success string";  // → 框架包装为 { output: "..." }
}

// 失败：throw Error（框架捕获后设置为 metadata.error）
async execute(args, context) {
  throw new Error("tool failed");
}
```

**框架包装后的最终格式**（传递给 after-hook 的 `output` 参数）：

```typescript
output = {
  title: string,        // 工具标题
  output: string,       // 工具 execute() 的返回值（JSON.stringify 后）
  metadata: {
    error?: string,    // 只有当工具 throw Error 时才有值
  }
}
```

### 2.3 内置工具返回值

OpenCode 内置工具（如 `read`, `Read`, `glob`, `Grep`, `Bash`, `Edit`, `Write`）：

```typescript
// 成功：直接返回文本内容
{
  output: "<文件内容或搜索结果>",
  title: "<工具名> - <目标>",
  metadata: {}
}

// 失败：metadata.error 有值
{
  output: "Error: <错误信息>",
  title: "<工具名> - <目标>",
  metadata: { error: "<错误信息>" }
}
```

---

## 三、失败检测逻辑

### 3.1 `detectFailure(output, tool?)` 函数

**文件**: `.opencode/service/enforcement/tool-tracker.ts` (lines 160-184)

```typescript
function detectFailure(output: any, tool?: string): { failed: boolean; error: string } {
  if (!output) return { failed: false, error: "" };

  const meta = output.metadata || {};

  // 1. metadata.error 是可靠信号 — 总是检查
  if (meta.error) return { failed: true, error: String(meta.error).slice(0, 300) };

  // 2. 对于只读工具，ONLY 检查 metadata.error
  // 不扫描 output content — 文件内容可能包含 "error", "fail" 等，导致误报
  if (tool && isReadOnlyTool(tool)) {
    return { failed: false, error: "" };
  }

  // 3. 对于非只读工具，扫描 output.output 和 output.title
  const text = typeof output.output === "string" ? output.output : "";
  const title = typeof output.title === "string" ? output.title : "";

  for (const p of ERROR_PATTERNS) {
    if (p.test(text)) return { failed: true, error: text.slice(0, 300) };
    if (p.test(title)) return { failed: true, error: `${title} | ${text.slice(0, 200)}` };
  }

  return { failed: false, error: "" };
}
```

### 3.2 `ERROR_PATTERNS` 完整列表

**文件**: `.opencode/service/enforcement/tool-tracker.ts` (lines 144-158)

```typescript
const ERROR_PATTERNS: RegExp[] = [
  /error:/i,           // "error:"（不区分大小写）
  /Error:/,              // "Error:"（首字母大写）
  /FAIL(?:ED)?\b/,     // "FAIL" 或 "FAILED"
  /\bBLOCKED\b/,        // "BLOCKED"（整词匹配）
  /internal error/i,     // "internal error"
  /timeout/i,           // "timeout"
  /EACCES/,              // "EACCES"（文件权限错误）
  /ENOENT/,              // "ENOENT"（文件不存在）
  /Cannot read propert/,  // "Cannot read propert..."（undefined 访问）
  /is not a function/,   // "is not a function"
  /TypeError:/,          // "TypeError:"
  /ReferenceError:/,     // "ReferenceError:"
  /split.*undefined/i,   // "split...undefined"（常见运行时错误）
];
```

### 3.3 `detectSoftRejection(output)` 函数

**文件**: `.opencode/plugin-handlers/after/anti-bypass.ts` (lines 17-38)

```typescript
function detectSoftRejection(output: any): { rejected: boolean; reason: string } {
  if (!output) return { rejected: false, reason: "" };

  // 1. MCP 标准错误格式
  if (output.isError === true) return { rejected: true, reason: "isError=true" };

  // 2. 解析嵌套 output（output.output 是 JSON 字符串）
  const innerText = typeof output.output === "string" ? output.output : "";
  let inner: any = null;
  try { inner = JSON.parse(innerText); } catch {}

  // 3. 检查顶层 status
  if (output.status === "rejected") return { rejected: true, reason: output.reason || "rejected" };
  if (inner?.status === "rejected") return { rejected: true, reason: inner.reason || "rejected" };

  // 4. 检查错误模式
  if (innerText.includes('"session not found"')) return { rejected: true, reason: "session not found" };
  if (inner?.error) return { rejected: true, reason: String(inner.error).slice(0, 200) };
  
  // 5. 检查 verified=false（attest 工具）
  if (inner?.verified === false && inner?.error) return { rejected: true, reason: "verified=false: " + String(inner.error).slice(0, 150) };

  return { rejected: false, reason: "" };
}
```

### 3.4 关键区分逻辑

```typescript
// after/anti-bypass.ts — 互斥关系
const { failed } = recordResult(sessionId, agent, tool, output);  // detectFailure() 先检查

if (!failed) {  // 只有 !failed 才走这里
  const softReject = detectSoftRejection(output);
  if (softReject.rejected) {
    const rejCount = recordSoftRejection(sessionId, tool, softReject.reason);
    // 如果 softRejCount >= 3 → before-hook 会阻断
  }
}
```

**流程图**：
```
工具执行完成
      ↓
recordResult() → detectFailure()
      ↓
  ┌──────────┬──────────┐
  │ failed    │ !failed   │
  │ (true)   │ (false)  │
  └──────────┼──────────┘
             ↓
     recordSoftRejection() → detectSoftRejection()
             ↓
        ┌──────────┬──────────┐
        │ rejected  │ !rejected │
        │ (true)   │ (false)  │
        └──────────┼──────────┘
```

---

## 四、计数器与阈值

### 4.1 四种计数器

| 计数器 | 追踪内容 | 递增条件 | 重置条件 | 阈值 |
|----------|-----------|-----------|-----------|--------|
| **consecutive_failures** | 连续工具失败 | `detectFailure()` 返回 `failed=true`（通过 `recordResult()`）或 `recordBlock()` 被调用（before-hook 阻断） | **永不重置**（除非 `clearGuidance()` / `clearAwaitingGuidance()` 被调用或 TTL 过期） | `softThreshold=2`（注入 STOP）<br>`hardThreshold=4`（阻断） |
| **total_failures** | 会话总失败次数 | 每次 `detectFailure()` 返回 `failed=true` 或 `recordBlock()` 被调用 | TTL 过期时重置 | `totalLimit=15`（无条件阻断） |
| **compliance_blocks** | 框架合规阻断次数（orphan before-hooks） | `recordAttempt()` 检测到 orphan before-hook（`last_before_at > last_after_at`） | **工具执行成功后重置**（`recordResult()` 中 `compliance_blocks = 0`） | `complianceThreshold=3`（阻断） |
| **soft_rejections** | 每个工具的软拒绝次数（per-tool） | `detectSoftRejection()` 返回 `rejected=true`（通过 `recordSoftRejection()` 写入独立表） | `resetSoftRejections()` 被调用时删除该 session 的所有记录（注：`clearGuidance()` **不**直接重置 `soft_rejections`） | `>= 3`（`before/anti-bypass.ts` 检查） |

### 4.2 Orphan 检测逻辑

**文件**: `.opencode/service/enforcement/tool-tracker.ts` (lines 261-276)

```typescript
// ── Orphan detection: previous before-hook fired but no after-hook ──
// This means another plugin blocked the previous tool (framework compliance block)
let complianceBlocks = row.compliance_blocks || 0;
if (row.last_before_at > row.last_after_at && row.last_before_at > 0) {
  complianceBlocks++;
  db.run(
    `UPDATE tool_enforcement SET compliance_blocks = ?, updated_at = ? WHERE session_id = ?`,
    [complianceBlocks, now, sessionId]
  );
  writeLog(SRC, "WARN", {
    event: "ORPHAN-DETECTED",
    sessionId, agent, tool,
    complianceBlocks,
    detail: `Previous tool blocked by framework compliance (orphan before-hook)`,
  });
}
```

**解释**：
- `last_before_at`：最后一次 before-hook 执行的时间戳
- `last_after_at`：最后一次 after-hook 执行的时间戳
- 如果 `last_before_at > last_after_at` → 说明 before-hook 执行了，但 after-hook 没执行（工具被其他 plugin 阻断）
- 这被计为 **框架合规阻断**（`compliance_blocks++`）

### 4.3 阈值检查与行为

| 阈值类型 | 配置键 | 默认值 | 触发行为 |
|-----------|-----------|---------|-----------|
| **softThreshold** | `enforcement.tool_tracker.soft_threshold` | 2 | 注入 STOP 指令到 Agent prompt（通过 `system/anti-bypass.ts`） |
| **hardThreshold** | `enforcement.tool_tracker.hard_threshold` | 4 | before-hook throw，阻断下一个工具调用 |
| **totalLimit** | `enforcement.tool_tracker.total_limit` | 15 | 无条件阻断 session |
| **complianceThreshold** | `enforcement.tool_tracker.compliance_threshold` | 3 | before-hook throw，阻断下一个工具调用 |

### 4.4 重置条件总结

| 计数器 | 重置条件 |
|----------|-----------|
| **consecutive_failures** | `clearGuidance()` / `clearAwaitingGuidance()` 被调用，或 TTL 过期 |
| **total_failures** | TTL 过期时重置 |
| **compliance_blocks** | **工具执行成功后重置**（`recordResult()` 中 `compliance_blocks = 0`） |
| **soft_rejections** | `resetSoftRejections()` 被调用时删除该 session 的所有记录（注：`clearGuidance()` 不直接重置） |

**关键发现**：
- `consecutive_failures` 和 `total_failures` **不会在工具成功执行后重置**
- 这是有意设计：**累积失败计数**贯穿整个 session 生命周期
- `clearGuidance()` / `clearAwaitingGuidance()` 会重置 `consecutive_failures` 和 `compliance_blocks`，但**不**重置 `soft_rejections`
- `soft_rejections` 是 per-tool 独立表，需通过 `resetSoftRejections()` 单独重置

---

## 五、各工具的实际返回格式（37 个自定义工具）

### 5.1 安全包装工具（8 个）

#### safe_edit

```
// 成功：（字符串）
"File edited successfully (backup: /path/to/backup)"

// 失败：throw Error
throw new Error(`safe_edit failed: ${result.error}`);
```

**特殊**: `dryRun` 模式返回 `"Validated patch/overwrite mode for: /path"`

#### safe_shell

```
// 成功：（JSON 字符串）
JSON.stringify({
  output: result.stdout || "",
  metadata: { exitCode: result.exitCode, stderr: result.stderr, ... }
})

// 失败：throw Error
throw new Error(`safe_shell blocked: ${result.blockedReason}`);
```

#### safe_delete / safe_restore / safe_mkdir

```
// 成功：（字符串）
"File deleted (backup: ...)"
"Restored ... from backup ..."
"Directory created: ..."

// 失败：throw Error
```

#### safe_diff

```
// 成功：（字符串，含 diff）
"[safe_diff] 3 line(s) added, 1 removed\n--- a/file\n+++ b/file\n..."

// 无差异时：
"[safe_diff] Content matches existing file"

// 失败：throw Error
```

#### safe_hash

```
// 成功：（JSON 字符串）
JSON.stringify({ file: "...", sha256: "abc123..." })

// 失败：throw Error
```

#### safe_test

```
// 成功：（字符串）
"Validation passed for task-123 (red phase)"

// 失败：throw Error
throw new Error(`safe_test validation failed: ${result.violations}`);
```

---

### 5.2 框架控制工具（5 个）

#### dispatch_subagent

```
// 成功：（字符串：包装后的 prompt）
return result.prompt;  // 含 DISPATCH_TOKEN 和 P0 协议

// 失败：throw Error
```

#### checklist_status / advance_checklist_phase

```
// 成功：（JSON 字符串）
JSON.stringify({
  run_id: "checklist-run-abc123",
  phase: "initial_read",
  status: "blocked",
  pending_blockers: [...],
  next_action: "..."
})
```

#### resolve_domain_id / config_read_attest

```
// 成功：（JSON 字符串）
JSON.stringify({
  domain_id: "opencode_framework",
  resolved_from: "session_map_db",
  ...
})
```

---

### 5.3 UC7KS 知识工具（5 个）

#### knowledge_cache_search

```
// 成功：（对象，框架自动包装）
{ output: JSON.stringify({ domain: "...", hits: [...], ... }) }

// 特殊处理：如果返回字符串，直接包装为 { output: result }
if (typeof result === "string") { return { output: result }; }
```

#### knowledge_cache_attest ⚠️ 软拒绝示例

```
// 成功：（对象）
{ output: JSON.stringify({ verified: true, session_id: "...", ... }) }

// 失败：（verified: false）← 软拒绝，不 throw
{ output: JSON.stringify({
    verified: false,
    error: "Missing read audit records for 2 skill file(s)",
    unread_files: [...],
    hint: "Call read_skill() before attesting"
  }) }
```

**关键**: 此工具**不 throw Error**，而是通过 `verified: false` 返回失败。

#### knowledge_gap_report / module_scope_declare / janitor

```
// 统一格式：返回 JSON 字符串
JSON.stringify(result)
```

---

### 5.4 Read Attestation 工具（3 个）

#### read_skill

```
// 成功：（对象）
{ output: "Full skill content here..." }

// 失败：（对象，不 throw）
{ output: "ERROR: Skill not found. Available: ..." }
```

**特殊**: 此工具**不 throw Error**，而是通过 `output` 字段返回错误信息。

#### skill_read_attest / rule_read_attest

```
// 成功：（JSON 字符串）
JSON.stringify({ attested: true, session_id: "...", ... })

// 失败：（attested: false）← 软拒绝，不 throw
JSON.stringify({ attested: false, error: "...", ... })
```

---

### 5.5 其他工具（2 个）

#### nightly-compaction

```
// 成功：（字符串）
"Nightly compaction completed. Compressed X sessions."

// 失败：throw Error
```

#### tsc_gate_reset

```
// 成功：（字符串）
"TSC gate reset. All tasks unblocked."

// 失败：throw Error
```

---

## 六、MCP 工具返回格式（25+ 个）

### 6.1 compliance-gate（9 个工具）

**MCP 标准格式**:

```typescript
{ content: [{type: "text", text: JSON.stringify(result)}], isError: boolean }
```

| 工具 | 成功条件 | isError |
|------|-----------|---------|
| `compliance_gate_check` | `passed=true` | `!passed` |
| `compliance_gate_confirm` | `status="armed"` | `status!="armed"` |
| `compliance_gate_complete` | `status="completed"` | `status!="completed"` |
| `compliance_gate_submit_deliverables` | `status="delivered"` | `status!="delivered"` |
| `compliance_gate_approve_deliverables` | `status="approved"` | `status=="rejected"` |
| `compliance_gate_purge` | 始终 | `false` |
| `compliance_gate_drain_stale` | 始终 | `false` |

---

### 6.2 notify-server（2 个工具）

#### acp_notify

```
// 成功：ok = true, isError = false
{ content: [{type: "text", text: '{"ok":true,"seq":42,...}'}], isError: false }

// 失败：ok = false, isError = true
{ content: [{type: "text", text: '{"ok":false,...}'}], isError: true }
```

#### clear_guidance

```
// 成功：ok = true, isError = false
// 失败：ok = false, isError = true
```

---

### 6.3 eslint-audit（1 个工具）

#### run_audit

```
// 成功：status = "pass" 或 "fail"（不 throw，isError 始终为 false）
{ content: [{type: "text", text: JSON.stringify({
      status: "pass",  // 或 "fail"
      violations: [],
      violations_count: 0,
      ...
  })}], isError: false }  // 注意：始终为 false
```

**特殊**: `eslint-audit` 工具**不返回错误**，`isError` 始终为 `false`。

---

### 6.4 其他 MCP 工具（13+ 个）

| MCP Server | 工具示例 | 返回格式 |
|------------|----------|-----------|
| `codegraph` | `codegraph_query`, `codegraph_impact` | MCP 标准格式 |
| `context7` | `context7_query`, `context7_resolve` | MCP 标准格式 |
| `knowledge-cache` | `knowledge_cache_search`, `knowledge_cache_attest` | MCP 标准格式 |
| `skill-manager` | `read_skill`, `list_skills` | MCP 标准格式 |

---

## 七、完整工具清单与返回格式速查表

### 7.1 自定义工具（23 个）

| # | 工具 | 成功返回格式 | 失败返回格式 | 特殊说明 |
|---|------|---------------|---------------|-----------|
| 1 | `safe_edit` | `string`: "File edited..." | throw Error | dryRun 可用 |
| 2 | `safe_shell` | `string(JSON)`: `{output,metadata}` | throw Error | dryRun 可用 |
| 3 | `safe_delete` | `string`: "File deleted..." | throw Error | — |
| 4 | `safe_restore` | `string`: "Restored..." | throw Error | UUID-based |
| 5 | `safe_mkdir` | `string`: "Directory created..." | throw Error | dryRun 可用 |
| 6 | `safe_diff` | `string`: "[safe_diff] ..." | throw Error | 返回 diff 文本 |
| 7 | `safe_hash` | `string(JSON)`: `{file,sha256}` | throw Error | 只读 |
| 8 | `safe_test` | `string`: "Validation passed..." | throw Error | dryRun 可用 |
| 9 | `dispatch_subagent` | `string`: prompt | throw Error | 返回 prompt |
| 10 | `checklist_status` | `string(JSON)` | 返回 `status:error` | — |
| 11 | `advance_checklist_phase` | `string(JSON)` | 返回 `advanced:false` | — |
| 12 | `resolve_domain_id` | `string(JSON)` | 返回 `domain_id:null` | — |
| 13 | `config_read_attest` | `string(JSON)` | 返回 `attested:false` | — |
| 14 | `knowledge_cache_search` | `object`: `{output:...}` | throw Error | — |
| 15 | `knowledge_cache_attest` | `object`: `{output:...}` | 返回 `verified:false`（软拒绝） | **不 throw** |
| 16 | `knowledge_gap_report` | `string(JSON)` | throw Error | — |
| 17 | `module_scope_declare` | `string(JSON)` | throw Error | — |
| 18 | `janitor` | `string`: "Janitor completed..." | throw Error | — |
| 19 | `nightly-compaction` | `string`: "Nightly compaction..." | throw Error | — |
| 20 | `tsc_gate_reset` | `string`: "TSC gate reset..." | throw Error | — |
| 21 | `read_skill` | `object`: `{output:"..."}` | 返回 `output: "ERROR:..."`（不 throw） | **不 throw** |
| 22 | `skill_read_attest` | `string(JSON)` | 返回 `attested:false`（软拒绝） | **不 throw** |
| 23 | `rule_read_attest` | `string(JSON)` | 返回 `attested:false`（软拒绝） | **不 throw** |

---

### 7.2 MCP 工具（25+ 个）

| # | MCP Server | 工具 | isError 逻辑 |
|---|------------|------|---------------|
| 1 | `compliance-gate` | `compliance_gate_check` | `!result.passed` |
| 2 | `compliance-gate` | `compliance_gate_confirm` | `result.status!="armed"` |
| 3 | `compliance-gate` | `compliance_gate_complete` | `result.status!="completed"` |
| 4 | `compliance-gate` | `compliance_gate_submit_deliverables` | `result.status!="delivered"` |
| 5 | `compliance-gate` | `compliance_gate_approve_deliverables` | `result.status=="rejected"` |
| 6 | `compliance-gate` | `compliance_gate_purge` | `false`（始终） |
| 7 | `compliance-gate` | `compliance_gate_drain_stale` | `false`（始终） |
| 8 | `notify-server` | `acp_notify` | `!result.ok` |
| 9 | `notify-server` | `clear_guidance` | `!result.ok` |
| 10 | `eslint-audit` | `run_audit` | `false`（始终） |
| 11+ | `codegraph` | `codegraph_query`, `codegraph_impact`, ... | 取决于具体实现 |
| 11+ | `context7` | `context7_query`, `context7_resolve`, ... | 取决于具体实现 |
| 11+ | `knowledge-cache` | `knowledge_cache_search`, ... | 取决于具体实现 |

---

## 八、完整失败类型矩阵

### 8.1 失败类型详解

| # | 失败类型 | 触发条件 | 检测函数 | 计数器 | 阈值阻断 | 示例 |
|---|-----------|-----------|-----------|---------|-----------|------|
| **A** | **硬失败（metadata.error）** | `output.metadata.error` 有值 | `detectFailure()` | `consecutive_failures`<br>`total_failures` | `>= hardThreshold(4)` | MCP 标准错误 |
| **B** | **硬失败（ERROR_PATTERNS）** | `output.output` 或 `output.title` 匹配 `ERROR_PATTERNS` | `detectFailure()` | `consecutive_failures`<br>`total_failures` | `>= hardThreshold(4)` | `"Error: session not found"` |
| **C** | **软拒绝（isError:true）** | `output.isError === true` 且无 `ERROR_PATTERNS` 关键词 | `detectSoftRejection()` | `soft_rejections` | `>= 3` | `{"isError":true,"output":"rejected"}` |
| **D** | **软拒绝（status:rejected）** | `output.status === "rejected"` 或 `inner.status === "rejected"` | `detectSoftRejection()` | `soft_rejections` | `>= 3` | `{"status":"rejected","reason":"..."}` |
| **E** | **软拒绝（verified:false）** | `inner?.verified === false` 且 `inner?.error` 有值 | `detectSoftRejection()` | `soft_rejections` | `>= 3` | `{"verified":false,"error":"..."}` |
| **F** | **框架阻断（before-hook throw）** | before-hook throw → 工具不执行 | `recordAttempt()` (orphan 检测) | `compliance_blocks` | `>= complianceThreshold(3)` | `Error: [FW-ENFORCE][PHASE-0] Tool "xxx" is BLOCKED during initial_read phase...` |
| **G** | **会话总失败上限** | `total_failures >= totalLimit` | `recordAttempt()` | `total_failures` | `>= totalLimit(15)` | 无条件阻断 session |

### 8.2 失败处理流程图

```
┌─ 工具执行完成 ──────────────────────────────────────────────────────────────────────┐
│                                                                               │
│  after/anti-bypass.ts handle()                                               │
│  ┌─────────────────────────────────────────────────────────────────────────────┐  │
│  │ const { failed, count, error } = recordResult(...);  // detectFailure()  │  │
│  │                                                                       │  │
│  │ if (failed) {                                                          │  │
│  │   → 硬失败（A 或 B）                                               │  │
│  │   → consecutive_failures++                                                │  │
│  │   → total_failures++                                                    │  │
│  │   → 检查阈值：                                                       │  │
│  │     if (consecutive >= hardThreshold) → 下次 before-hook 会阻断         │  │
│  │ } else {                                                               │  │
│  │   → 成功或软拒绝                                                      │  │
│  │   → compliance_blocks = 0（重置）                                      │  │
│  │                                                                       │  │
│  │   const softReject = detectSoftRejection(output);                        │  │
│  │   if (softReject.rejected) {                                           │  │
│  │     → 软拒绝（C 或 D 或 E）                                       │  │
│  │     → soft_rejections++（per-tool）                                    │  │
│  │     → 检查阈值：                                                       │  │
│  │       if (softRejCount >= 3) → 下次 before-hook 会阻断             │  │
│  │   }                                                                   │  │
│  │ }                                                                       │  │
│  └─────────────────────────────────────────────────────────────────────────────┘  │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────────────┘

┌─ 下一次工具调用 ──────────────────────────────────────────────────────────────────┐
│                                                                               │
│  before/anti-bypass.ts handle()                                              │
│  ┌─────────────────────────────────────────────────────────────────────────────┐  │
│  │ // 1. 检查 guidance gate                                               │  │
│  │ if (getAwaitingPhase(sessionId) === 1) {                               │  │
│  │     if (isClearGuidanceTool(tool) || isReportTool(tool)) → 允许       │  │
│  │     else → throw（阻断）                                              │  │
│  │ }                                                                       │  │
│  │                                                                       │  │
│  │ // 2. 检查 soft rejection 阈值（v37）                                │  │
│  │ const softRejCount = getSoftRejectionCount(sessionId, tool);              │  │
│  │ if (softRejCount >= 3) {                                              │  │
│  │   throw（阻断）                                                        │  │
│  │ }                                                                       │  │
│  │                                                                       │  │
│  │ // 3. 检查 cumulative failures                                         │  │
│  │ const { count, shouldBlock, reason } = recordAttempt(...);                │  │
│  │ if (shouldBlock) {                                                      │  │
│  │   → consecutive_failures >= hardThreshold                                 │  │
│  │   → OR compliance_blocks >= complianceThreshold                            │  │
│  │   → OR total_failures >= totalLimit                                     │  │
│  │   throw（阻断）                                                        │  │
│  │ }                                                                       │  │
│  └─────────────────────────────────────────────────────────────────────────────┘  │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────────────┘
```

### 8.3 关键区别总结

| 区别点 | 硬失败（A/B） | 软拒绝（C/D/E） | 框架阻断（F） |
|---------|-------------------|-------------------|-------------------|
| **检测函数** | `detectFailure()` | `detectSoftRejection()` | `recordAttempt()` (orphan 检测) |
| **计数器** | `consecutive_failures`<br>`total_failures` | `soft_rejections` (per-tool) | `compliance_blocks` |
| **重置条件** | `clearGuidance()` / `clearAwaitingGuidance()` 或 TTL | `resetSoftRejections()` 单独重置 | **工具成功执行后重置** |
| **阈值** | `hardThreshold=4`<br>`totalLimit=15` | `>= 3` (per-tool) | `complianceThreshold=3` |
| **工具是否执行** | 是（工具执行了但返回错误） | 是（工具执行了但返回拒绝） | **否**（before-hook throw） |
| **是否触发 after-hook** | 是 | 是 | **否** |

---

## 九、附录：关键代码索引

### 9.1 核心文件列表

| 文件路径 | 内容 | 行数 |
|-----------|------|------|
| `.opencode/service/enforcement/tool-tracker.ts` | 核心强制执行逻辑：`detectFailure()`, `detectSoftRejection()`, `recordResult()`, `recordAttempt()`, `checkThreshold()`, `rewardReport()`, `clearGuidance()` | ~875 |
| `.opencode/plugin-handlers/before/anti-bypass.ts` | Before-hook 强制执行：Guidance Gate 检查、阈值检查、框架阻断 | ~143 |
| `.opencode/plugin-handlers/after/anti-bypass.ts` | After-hook 失败检测：`detectSoftRejection()` 调用、reward 逻辑 | ~93 |
| `.opencode/plugin-handlers/system/anti-bypass.ts` | System transform：注入 STOP 指令、Guidance Gate 两阶段协议 | 待确认 |
| `.opencode/project.config.json` | 中央配置：`enforcement.tool_tracker` 部分 | 待确认 |

### 9.2 配置项详解

```json
{
  "enforcement": {
    "tool_tracker": {
      "soft_threshold": 2,          // cumulative failures → 注入 STOP 指令
      "hard_threshold": 4,          // cumulative failures → before-hook throw
      "total_limit": 15,             // 总会话失败上限 → 无条件阻断
      "compliance_threshold": 3,     // 框架合规阻断 → before-hook throw
      "read_only_block": false,      // 只读工具失败是否阻断（通常 false）
      "ttl_ms": 3600000,            // Session 状态 TTL（1 小时）
      "read_only_tools": [...]        // 覆盖默认只读工具列表
    }
  }
}
```

### 9.3 设计模式总结

#### 9.3.1 成功返回模式

| 模式 | 使用工具 | 示例 |
|-------|-----------|------|
| **字符串直接返回** | `safe_edit`, `safe_delete`, ... | `"File edited successfully"` |
| **JSON 字符串返回** | `safe_shell`, `safe_hash`, ... | `JSON.stringify({status: "armed", ...})` |
| **对象返回（框架包装）** | `knowledge_cache_search`, ... | `{output: JSON.stringify(result)}` |
| **MCP 标准格式** | 所有 MCP 工具 | `{content: [...], isError: false}` |

#### 9.3.2 失败返回模式

| 模式 | 使用工具 | 示例 |
|-------|-----------|------|
| **throw Error** | 大多数自定义工具 | `throw new Error("...")` |
| **返回 verified:false** | `knowledge_cache_attest`, ... | `JSON.stringify({verified:false, ...})` |
| **返回 output 含 ERROR:** | `read_skill` | `{output: "ERROR: Skill not found"}` |
| **MCP isError:true** | 大多数 MCP 工具 | `{content: [...], isError: true}` |

### 9.4 更新记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-02 | v1.0.0 | 初始版本，基于 work-one 项目代码分析 |
| 2026-07-03 | v2.0.0 | 整合 `opencode-tool-return-format.md`，增加完整工具返回格式详情 |

---

**注**: 本文档基于 work-one 项目当前代码状态生成。若代码更新，请同步更新此文档。

**文档状态**: ✅ 完整（基于实际代码验证）
