# Git Write Grant 继续实施说明

## 执行标识
- 执行需求唯一ID: `resp-20260708-001`
- 执行时间戳: `2026-07-08`
- 执行范围: `GitHub MCP 写 active hook` + `repo grant runtime cleanup`

## 执行内容
- 保留 `work-one/opencode.json` 中 `explore.model=opencode-go/glm-5.2` 的未提交漂移，不在本轮改动。
- 为 direct `github_*` MCP 写工具补 active `codegraph` before-hook 阻断，读工具继续放行。
- 为 DB cleanup 补 repo grant 过期撤销、终态 grant 清理、repo audit event 清理。
- 补充自动化测试并同步计划/验收/审核文档状态。

## 修改的代码路径
- `/home/zhaoge/workspace/opencode/work-one/.opencode/plugin-handlers/before/codegraph.ts`
- `/home/zhaoge/workspace/opencode/work-one/.opencode/plugins/before-dispatcher.ts`
- `/home/zhaoge/workspace/opencode/work-one/.opencode/lib/db-maintenance.ts`

## 生成的代码路径
- `/home/zhaoge/workspace/opencode/work-one/.opencode/plugin-handlers/before/__tests__/codegraph.test.ts`
- `/home/zhaoge/workspace/opencode/work-one/.opencode/lib/__tests__/db-maintenance.test.ts`

## 实现结果
- `github_get_* / github_list_* / github_search_*` 在 active hook 中按只读路径放行。
- `github_create_* / github_update_* / github_merge_* / github_add_*` 在 active hook 中 fail-closed，并要求改走 `safe_gh_*` / `safe_repo_push` + `remote_repo_write` grant。
- `runAuditCleanup()` 现在会撤销 expired `pending/bound` repo grants，并清理过旧 `consumed/revoked` grants 与 `repo_operation_events`。
- 定向测试 `bun test` 结果为 `74/74 PASS`。

## 配置方式
- 无新增配置项。
- `before-dispatcher` 新增 `github_*` 前缀匹配，使 `codegraph` handler 能覆盖 GitHub MCP 工具。
- DB 清理继续复用现有 `session` / `db-health` 调用链，无需额外接线。

## 注意事项
- direct `github_*` 写路径的 runtime smoke 已在后续同日补齐；需附带“验证前必须重启 serve daemon”的运行说明。
- `remote positive live E2E` 仍未执行；如需更高证据，建议单独授权后跑 `safe_repo_push(dryRun=true)`。
- `grant` 创建期的 runtime/test 路径校验仍可继续加固，但不影响本轮 active cleanup 生效。
